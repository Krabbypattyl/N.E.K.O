# N.E.K.O 小剧场实测问题描述以及解决方案

状态：当前实测问题、解决方向与质量闭环记录

本文是 N.E.K.O 仓库内小剧场唯一的实测问题文档。这里只记录有代码链、日志、Session/Ledger 或真实前端回放证据的问题；产品与技术合同见[小剧场架构开发文档](./neko-theater-architecture.md)。

## 1. 记录规则

每个问题必须包含：

1. 可复现输入或操作路径；
2. Story ID、Session ID、revision 或其他证据锚点；
3. 玩家实际看见的结果与正式状态；
4. 按 Story Package → Session/Ledger → Evaluator/Actor 输入输出 → Runtime/Store → 前端的顺序归因；
5. 解决方案、禁止采用的错误修法和回归标准；
6. `待修复`、`已修复待复测` 或 `实测通过` 状态。

模型 Prompt 中已经写了要求，不等于问题已经解决。只有代码约束、自动回归和真实模型复测共同闭环后，才能标记为“实测通过”。

## 2. 当前未解决问题

### 问题 1：正式节点已推进，但可见场景没有切换

状态：**已修复待真实前端复测**
发现日期：2026-08-14

#### 2.1 玩家现象

玩家在暴雨夜被允许暂住后，正式剧情本应进入“合租第二天的秋刀鱼晚餐”。页面没有出现时间和场景过渡，Actor 仍围绕暴雨夜、客厅、毛毯、剪刀和烧水继续演绎。

玩家随后明确输入：

- `行行行，我整点热水喝了就休息了`
- `睡了`
- `保持沉默，直接休息`

Actor 虽然描写了短暂安静，却继续输出：

- `……最好是真的睡了。要是敢半夜偷东西，我就剪断你的手指。`
- `……装睡也没用。我听得见你的呼吸声。`

并继续推荐“保持沉默，继续装睡”“询问她是否也去休息”等旧场景交互。玩家已经明确结束当晚交流，演绎仍把他拉回同一段对话，没有平滑过渡到第二天。

#### 2.2 可复现证据

- Story ID：`story_3e874621e937`
- Session ID：`numeric_web_session_a7a29c4d-a1bf-4713-87b8-8e8d3c24ed77`
- Session 文件：`~/Library/Application Support/N.E.K.O/theater/numeric_v2/sessions/numeric_web_session_a7a29c4d-a1bf-4713-87b8-8e8d3c24ed77.json`
- 文件更新时间：2026-08-14 10:04:05 +0800
- 最终快照：`status=active`、`revision=12`、`current_node_id=mainline_02`、`node_turn_count=7`

关键 Ledger：

| revision | 玩家输入 | 正式节点 | `scene_complete` | `route_status` |
| ---: | --- | --- | --- | --- |
| 4 | `所以今晚我是能呆在这了吧` | `mainline_01 → mainline_01` | false | `scene_incomplete` |
| 5 | `这大雨天的哪有光` | `mainline_01 → mainline_02` | true | `advanced` |
| 10 | `行行行，我整点热水喝了就休息了` | `mainline_02 → mainline_02` | false | `scene_incomplete` |
| 11 | `睡了` | `mainline_02 → mainline_02` | false | `scene_incomplete` |
| 12 | `保持沉默，直接休息` | `mainline_02 → mainline_02` | false | `scene_incomplete` |

`mainline_02` 的正式作者目标是：

- 章节：`第二章：秋刀鱼的香气`
- 时间：合租第二天傍晚
- 场景：男主炙烤秋刀鱼并准备味噌汤
- 必须发生：女主走出房间，与男主共进晚餐，首次接受男主提供的食物

`route_mainline_01_02.transition_contract.must_deliver` 也要求明确换场到合租第二天、秋刀鱼和晚餐。revision 5 的 Actor 结果却仍是夜色渐深、暴雨、沙发和剪刀，没有建立目标节点开场。revision 6—12 虽然正式节点已经是 `mainline_02`，可见事实仍沿用旧雨夜，因此 Evaluator 一直无法从当前演绎确认“第二天共进秋刀鱼晚餐”的 `pending_goals`。

#### 2.3 已确认根因

这不是 Runtime 不会自动推进节点。revision 5 已证明 Runtime 正确选择 `route_mainline_01_02`，并把 Session 的正式节点改为 `mainline_02`。

问题发生在“正式推进”与“可见演绎”之间：

1. Router 向 Actor 传入了 `route_changed=true`、来源节点、目标节点、目标 `opening_scene` 和 `transition_contract`；
2. Actor Prompt 要求回应当前输入、收住来源互动并桥接目标开场；
3. Actor 输出解析器只校验 JSON、字段、对白 speaker 和文本形状，没有验证换场段是否存在或是否交付目标开场；
4. 该输出随后与已经推进到目标节点的 Session 一起原子提交；
5. Numeric 前端按当前产品约定不显示节点标题或场景卡，只显示 Actor 表现，因此玩家看不到 Runtime 已经换节点；
6. 下一轮 Evaluator 以 `mainline_02` 的目标判定，却只能看到 Actor 留下的旧场景事实，形成“正式节点向前、可见剧情卡在原地”的闭锁。

所以，这是 **Actor 换场交付没有成为提交不变量**，不是路由条件、`min_turns`、前端按钮或 Session 恢复问题。

#### 2.4 为什么不能用“识别睡觉后直接进下一幕”修复

玩家输入“睡了”时，Runtime 已经处于 `mainline_02`。如果把休息关键词直接解释成节点推进，会从尚未演出的秋刀鱼晚餐跳到 `mainline_03`，再次绕过作者 `must_happen`，并让 Runtime 承担自然语言意图分类。这会破坏既定模型权限和确定性路线合同。

休息类输入应按当前正式状态处理：

- 当前幕未完成：允许 Actor 收住即时交流、描写时间流逝或休息，但不能跳过本幕目标；
- 当前幕完成且 Runtime 已选择路线：休息、离开、结束交谈可以成为过渡到下一场景或第二天的自然桥；
- 不根据单个关键词新增隐式路线，也不让 Actor 自己决定目标节点。

#### 2.5 解决方案

方案目标是让“正式节点”和“玩家看见的场景”在同一个原子提交内保持一致，不增加 Planner、Repair、自动评分模型或第二次 Actor 调用。

1. **把换场输出结构化。** `route_changed=true` 时，同一次 Actor 输出必须分别提供来源回应、过渡桥和目标开场三个非空段；普通未换场回合继续使用现有简化结构。前端仍可按一段连续演绎展示，不增加章节卡。
2. **让解析失败阻止提交。** 缺少来源回应、过渡桥或目标开场时视为 Actor 合同失败，Session、Ledger 和表现历史都不提交；不能再提交“节点已换但换场正文缺失”的回合。
3. **记录可见节点锚点。** 换场成功的 performance record 保存有序 `segments`、`visible_node_id` 和 `transition_delivered`；Store 复验路线 Ledger 与表现记录一致。恢复时据此确认最后一条可见演绎已经锚定当前节点。
4. **目标开场输入保持确定性。** Actor 只消费 Runtime 已选定的目标 `opening_scene`、`must_deliver` 和 `must_preserve`，不能自行换目标；`target_opening.scene_narration` 必须以目标 `opening_scene` 原文开头，后续正文再自然延展。
5. **收紧休息后的推荐输入。** 玩家明确休息后，如果没有目标场景中的叫醒或紧急事件，不推荐“装睡、继续解释、继续聊天”等反向动作；若本幕仍有待完成目标，用自然时间过渡把演绎带到目标事件发生条件，而不是重复旧对话。
6. **增加软节奏预算。** 非结局节点可声明玩家不可见的 `recommended_turns`。接近预算时 Actor 聚焦本幕目标，达到或超过预算时主动收束重复交流；预算不设置 `scene_complete`、不触发路线，也不限制玩家继续自由输入。

实现保持单次 Actor 调用，没有增加 Planner、Repair、关键词路由或硬性自由发言次数限制，也没有用前端显示节点标题掩盖演绎缺口。

#### 2.6 实现与自动验证

- Actor 在 `route_changed=true` 时只接受 `source_response → transition_bridge → target_opening` 三段结构，缺段、乱序或目标开场不匹配都会失败；
- Runtime 在提交前校验 `transition_delivered`、`visible_node_id` 和 Ledger 目标节点，Store 对带新 performance 合同版本的记录在加载与恢复时再次复验；无版本标记的修复前记录保持兼容；
- 前端按段落顺序渲染换场表现，普通旧 performance 仍走兼容投影；
- `recommended_turns` 已进入 Numeric v2 合同、Actor 节奏提示和生成器节点编辑器；当前生成器默认 15、最大 40、不得小于 `min_turns`；
- N.E.K.O 合同、模型、Runtime、Router 和浏览器定向回归共 63 项通过；生成器合同、主线、支线和前端定向回归共 54 项通过；
- 作者项目 `project_301ef5cdee01` 已无模型重生成地更新为 revision 87；修复版 `story_24738dcf0099` 已通过 canonical 编译与 N.E.K.O 复验并安装，package hash 均为 `sha256:7d486957b30cb8055733b3959b3fa1cbcb67f91a6195ad4e5fc0e4518f9ded05`；
- 原包 `story_3e874621e937` 和其 active Session `numeric_web_session_a7a29c4d-a1bf-4713-87b8-8e8d3c24ed77` 保留，未做破坏性覆盖。
- 已用该真实旧 Session 验证兼容恢复：`mainline_02`、revision 12、`status=active` 均保持不变。

#### 2.7 真实复测标准

自动回归至少覆盖：

1. 构造 `mainline_01 → mainline_02` 的 route change，Actor 缺少任一换场段时整回合不提交；
2. 合法换场后 Ledger 的 `to_node_id`、Session `current_node_id` 与 performance 的可见目标锚点一致；
3. 刷新恢复后仍从目标场景继续，不能回到来源场景；
4. 使用本次输入序列回放，revision 5 的可见正文必须出现第二天、厨房/晚餐与秋刀鱼等目标开场事实；
5. 输入“睡了”或“直接休息”后，演绎自然收束，不继续推荐“装睡并交流”；
6. 不允许在晚餐目标未完成时直接推进 `mainline_03`；
7. Evaluator 仍只输出作者 criterion、有限强度、目标证据与 `scene_complete`，服务端确定性换算 delta，Runtime 仍只按作者路线推进，模型调用次数不增加；
8. 真实前端模型复测通过后，才能把状态改为“实测通过”。

### 问题 2：连续回合只有旁白，没有猫娘对白

状态：**已修复待真实前端复测**
发现日期：2026-08-14

#### 2.8 证据与根因

- Story ID：`story_24738dcf0099`
- Session ID：`numeric_web_session_265f1017-6163-4c6b-bc01-d2dd32a0df3f`
- revision 2—4 的 performance 均有旁白，但 `dialogue=[]`；前端和 TTS 因而没有可显示、可播放的对白。
- revision 2 仍处于软节奏 `normal`，所以问题不是软预算单独造成；revision 3、4 的 `focus/closure` 可能进一步强化动作和环境描写倾向。
- 旧 Actor DTO 把 `narration` 与 `dialogue[]` 分离，天然只能先显示全部旁白、再显示全部对白；解析器又允许“有旁白、无对白”提交，并会静默过滤非法对白项。
- 原始模型输出没有持久化，历史记录无法区分模型主动返回空数组，还是对白项被过滤，这是附带的可观测性缺口。

#### 2.9 解决方案与复测标准

- 新 Actor 输出改为单一 `performance` 混合正文：括号内是微动作，括号外是猫娘对白；动作和对白按实际发生顺序穿插，不限制固定对白句数；
- 普通回合必须同时包含至少一个括号动作和有效猫娘对白；括号不成对、嵌套括号、空文本或纯动作输出整回合失败，不再提交静默降级结果；
- 换场仍保留三段结构，但每段内部使用相同内容块；来源回应至少有一句对白；
- Actor 与 Evaluator 的历史上下文、前端恢复、TTS 提取都消费同一顺序；TTS 只朗读对白，并保持多句对白的先后次序；
- 旧 `narration/dialogue` 和旧换场段继续兼容显示与恢复，不重写已提交历史；
- 使用修复版剧本继续演绎，至少验证一个普通回合包含“动作 → 多句对白 → 动作 → 对白”，刷新后原始顺序不变，才能标记为“实测通过”。

### 问题 3：已进入新章节，演绎焦点却偏离章节标题

状态：**代码已修复待真实前端复测**

#### 2.10 证据与根因

- 《云舒镇的奇遇（节奏修复版）》已从 `mainline_01` 正确推进到 `mainline_02`，目标章节为“第二章：秋刀鱼的香气”；
- 目标开场同时出现青菜与秋刀鱼，玩家用“这个”指代时，Actor 选择青菜并将其写入正式历史，后续回合受连续性合同约束而继续青菜线；
- Runtime 与换场合同均正常，问题在于 Actor 只获得节点开场锚点与宽泛的 `pending_goals`，没有获得 `chapter` 标题，因而无法在多个合法焦点之间优先保持本幕主题。

#### 2.11 解决方案与复测标准

- Actor 开场输入增加 `current_chapter_title`，普通回合增加当前标题，换场回合同时增加 `current_chapter_title` 与 `target_chapter_title`；
- 标题只是软主题锚点：只在多个候选焦点同样成立且上下文未指定对象时参与取舍，不覆盖玩家输入、已发生事实或作者硬边界；
- Evaluator 和 Runtime 的职责不变，标题不参与 `scene_complete`、数值、路线或结局判定；
- 自动回归必须验证开场可见当前标题、换场可见来源与目标标题，且软锚点的优先级限制仍完整保留；
- 真实前端模型复测需使用同一幕同时包含主题道具与次要道具、玩家使用模糊指代的用例，确认 Actor 优先围绕章节标题所指主题展开。

### 问题 4：最新回合被平均裁剪，物品与权限状态断裂

状态：**代码已修复待真实前端复测**

#### 2.12 证据与根因

- Story ID：`story_24738dcf0099`；Session ID：`numeric_web_session_eaa07e82-d261-47b2-a619-cfa88995ff33`；
- revision 7 完整记录已建立“浴室禁止进入”、“旧毛巾在落灰储物柜”、“毛巾干燥但有霉味”和“玩家自行取用”；
- 旧 Prompt 装箱把开场与最近六轮的所有内容交给通用裁剪器，为了适配总预算而统一降低每个字符串上限；
- revision 8 实际模型 Prompt 中，最后一句只剩“那里有一条旧毛巾，虽然是”，储物柜位置和毛巾后续状态均丢失；
- Actor 因而自行补成“上周擦地板的毛巾”并将毛巾移到沙发扶手，同时在未明确授权的情况下生成“洗完之后把浴室门关上”及“走向浴室”推荐输入。

#### 2.13 解决方案与复测标准

- Actor 先计算系统合同和本轮固定数据占用，再把真实剩余预算分配给最多 2200 Token 的当前场景 `recent_context`；
- 最新一轮原文完整优先，开场锚点其次，较早回合从新到旧按完整句降级；不增加记忆模型或额外调用；
- 未换场回合删除重复的 source beat、空 target beat 和空过渡合同，为已发生记录让出预算；
- 使用真实 revision 7 快照和当前猫娘人格重建 revision 8 Prompt：总输入为 3170 Token，通用裁剪后 Human Prompt 与专用装箱结果完全一致，最新回合六个有序内容块原样保留；
- 自动回归必须校验储物柜、毛巾干燥但有霉味、玩家自行取用与浴室禁止进入四项事实全部进入裁剪后 Prompt；
- 真实前端复测必须证明 Actor 不再无过程改写毛巾用途、位置或浴室权限，通过后再改为“实测通过”。

#### 2.14 关联回归：上一轮内容压过当前输入

- 最新 Session `numeric_web_session_66102354-54e9-4768-ad19-87c8d34b3a67` 的 revision 10 收到了玩家输入“那我都吃咯”，但提交内容与 revision 9 的相似度为 70.25%，两句对白逐字相同且没有回应“都吃掉”；不同 `client_turn_id` 和连续 Ledger revision 证明这不是前端重复渲染或重复提交；
- 重建 Prompt 证明玩家输入没有丢失。根因是完整最新回合位于 Human JSON 末尾，短玩家输入位于它之前，上一轮内容获得了更强的末尾注意力；Actor 又只校验结构，没有拦截整轮近重复；
- 修复后 `recent_context` 位于前，本轮 `response_instruction` 与 `player_input` 固定放在最后；真实 Session 重建总输入为 3160 Token，通用裁剪未改写 Human Prompt，上一轮完整事实仍原样保留；
- 增加确定性提交保护：只有当本轮全部对白都照搬上一轮，且整轮正文相似度达到 65% 时才拒绝提交，避免把单句口头禅误判为整轮重复；拒绝时不增加第二次模型调用，不修改 Session、Ledger 或 revision。

### 问题 5：起手句式模板化，不同角色卡的演绎趋同

状态：**代码已优化待跨角色真实模型压测**

#### 2.15 证据与根因

- 最近四个可读取 Session 的 revision 2 首段分别使用“听到‘权利’二字”“听到‘中介’二字”“听到这句近乎无赖的宣言”和“听到那声无奈的叹息”等同构起手；最新 Session 的 revision 3 又出现“听到‘洗澡’二字”，因此不是第二回合固定分支；
- “近乎无赖的宣言”只存在于模型生成的 Session，不存在于 Story Package、Actor Prompt 或源码，具体措辞不是剧本写死；
- 推荐输入和自由输入最终都只向后端提交 `message`，Actor 不知道输入来源，两种输入方式不会触发不同文案模板；
- Story Package 确实规定了开场的高度警惕、剪刀和领地防御，因此敌视反应方向属于作者情境；但通用 Actor 合同长期强调“承接并直接回应玩家”，角色人格只作为较短的系统末尾片段，且没有近期句式提示，模型容易把所有猫娘收敛为相似的中文小说反应模板；
- 该问题不是单个禁词问题。若硬禁“听到”或“闻言”，模型只会替换为“面对这番话”等同义模板，同时进一步削弱角色自然表达。

#### 2.16 优化方案与复测标准

- 从通用 System Prompt 移出角色人格，把角色卡人格放入开场和每回合 Human Prompt 尾部的 `acting_context.core_persona`，使其靠近当前输入并保持 160 Token 上限；
- 确定性提取最近三轮首个内容块的首个完整分句作为 `recent_openings`，每条最多 40 Token，只提示模型避免连续复用相同起手结构、比喻和动作组合；不新增总结模型、持久化字段或模型调用；
- 增加软文风合同：角色卡决定措辞、句长、主动性和情绪外显方式；“听到”“闻言”不是禁词，旁白减少替作者评价玩家，且不能为求变化破坏人格和事实；
- 不增加基于回合号的模板轮换，也不因为单个句式相似而拒绝提交；现有近重复保护继续只处理整轮内容照搬；
- 自动回归必须验证不同角色人格会形成不同的 `acting_context.core_persona`、该字段靠近当前输入、最近起手句按完整句投影、总 Prompt 不超过 4800 Token，且最新事实记忆不被二次裁剪；
- 真实模型压测至少覆盖三张表达差异明显的角色卡、同一 Story 和同一输入，并连续演绎 8—10 轮；需要同时评分角色可辨识度、起手句多样性、人格稳定性和剧情连续性，通过后再改为“实测通过”。

### 问题 6：角色卡已加载，但剧本临时状态覆盖核心人格

状态：**小葵定向复测通过，仍需三角色卡差异复测**

#### 2.17 证据与根因

- 小葵角色卡的核心特质为“甜美治愈、元气满满、温柔体贴、粘人撒娇”，但最新 Session `numeric_web_session_f09e1595-5595-4927-b0eb-b12d3fe40ca7` 连续生成“滚出去”“更恶心”和“划破你的喉咙”等毒舌、暴力表达；
- 输出正确使用了角色卡自称“人家”，证明角色卡已经加载，但模型只提取了表面语言标记；Story Package 的 `role_overlay` 同时写有“极度警惕、敌视”，当前节点 `catgirl_situation` 又写有“高度应激、认为男主是威胁”，更具体的剧本描述覆盖了核心人格；
- 旧 Prompt 虽已把角色人格移到尾部，却仍平铺发送 `intro`、`role_overlay`、`scene_role_notes`、metric bands 和人格摘要，没有显式权限层级，也重复提供了会互相竞争的人格性描述；
- 剧情要求角色警惕和拒绝进入属于硬局势，但不要求角色使用羞辱、厌恶或致命威胁。相同局势应通过当前角色卡的人格表现。

#### 2.18 优化方案与复测标准

- 不新增 Story Package 字段，复用 `intro.catgirl_identity`、`role_overlay`、`catgirl_situation` 和 metric bands，统一投影为结构化 `acting_context`；
- `core_persona` 是唯一核心人格；`story_identity` 只提供身份经历，`story_role_context` 只提供剧情关系，场景状态和关系 band 都是临时调制层。冲突时必须以核心人格重新表达同一剧情局势；
- 从 Actor 普通字段中删除重复的 `role_overlay`、`current_metric_bands` 和 `scene_role_notes`，避免同一临时状态多次压过角色卡；换场时在同一个 `acting_context` 增加 `target_scene_state`；
- 关系 band 只调整信任、距离、亲密度和主动性；每轮根据 Session metrics 重建，跨 band 或换幕时自然更新，不修改角色卡、不新增持久化状态或模型调用；
- 使用小葵问题 Session 的 revision 2 快照重建 revision 3 Prompt：System 1252 Token、Human 1914 Token、总计 3166 Token；通用裁剪未改写 Human Prompt，最新一轮六个内容块原样保留，尾部字段依次为 `recent_context`、`recent_openings`、`acting_context`、`style_instruction`、`response_instruction`、`player_input`；
- 自动回归必须覆盖“温柔核心人格 + 毒舌剧情身份 + 敌视临时状态”的显式冲突，确认两个不同角色卡只改变 `core_persona`，剧情身份和临时状态保持一致，且优先级合同进入开场、普通回合和换场 Prompt；
- 小葵真实复测仍应保持“不信任陌生人、拒绝进入私人空间”等剧情事实，但不能再无依据生成羞辱性评价或致命暴力威胁；通过后再改为“实测通过”。

#### 2.19 2026-08-19 真实模型复测结果

本轮使用当前安装的三个剧本、当前小葵角色卡和真实模型 API，在隔离临时目录中运行 Session；临时 Session 结束后销毁，正式 Session 数量保持为 3。

角色卡核心人格仍未稳定压过剧情临时状态。可见输出包括：

- 《云舒镇的奇遇》：`这种借口人家听多了。付了房租又怎样，这屋子现在是我的。`、`既然坐着，就管好你的手和眼睛。`；
- 《猫尾特调恋爱指南》：`少来这套！`、`今天的工钱就全扣光！`、`去全镇贴告示通缉你！`。

这些输出保留了“小葵”“人家”等表层标记，却主要按“敌视、傲娇、强势”的剧情身份决定措辞。警惕、拒绝和边界仍属于正确剧情事实，但表现方式没有保留“甜美、温柔、粘人”的核心底色，因此问题 6 仍是 **待修复**，不能按已有 Prompt 和自动回归判定通过。

后续修复方向：

1. `story_role_context` 和 `current_scene_state` 只描述局势、目标和可观察行为，不再包含能够直接支配措辞风格的“毒舌、敌视、压榨”等人格命令；
2. Actor 明确规定只有 `core_persona` 可以决定用词、攻击性、亲昵称呼和情绪表达方式，剧情层只能决定“拒绝什么、保护什么、与玩家保持多远”；
3. 生成器检查 `role_overlay` 与节点状态，避免把永久人格词写进临时层；现有剧本也要同步清理这类描述，不能只改 Runtime Prompt；
4. 使用小葵、糖糖、雪奈三张差异明显的角色卡，对同一剧本、同一输入各运行至少 8 回合；只有剧情事实一致而表达方式明显可辨，并且小葵不再出现无依据羞辱或暴力威胁，才能关闭此问题。

后续完整链路复测再次确认该问题仍存在。《云舒镇的奇遇》中小葵用“跨过就咬你”表达领地边界；《猫尾特调恋爱指南》进入高亲密醉酒场景后说“要是你敢走，我就咬断你的脖子”。两处都可以保留警惕、占有欲或害怕被抛弃的剧情含义，但致命暴力威胁仍不符合“甜美治愈、温柔体贴、粘人撒娇”的核心人格，说明仅调整 `acting_context` 顺序和 Prompt 优先级还不足以阻止临时剧情身份支配攻击性。

最新修复把攻击性、称呼和情绪表达权限进一步收回 `core_persona`，明确禁止温柔、甜美、治愈型角色使用“敢……就……”“饶不了你”“舔干净”“用余生偿还”等威吓羞辱句；同时清理三个现有包中会支配文风的敌视、毒舌和压榨式临时描述。真实模型用当前小葵连续复测三个剧本，共 25 个成功回合、8 次换场，未再出现羞辱、致命威胁或强制关系表达。该结果关闭小葵当前反例，但尚未完成小葵、糖糖、雪奈三张角色卡的同场景差异评分，因此不把跨角色问题整体关闭。

### 问题 7：开场推荐输入被格式示例污染并跨剧本串线

状态：**已修复，真实模型定向复测通过**
发现日期：2026-08-19

#### 2.20 证据、根因与修复方向

真实压测中，《猫尾特调恋爱指南》的第一次开场推荐输入为：

`我也是被中介骗来的，而且已经付了房租。`

这属于《云舒镇的奇遇》的玩家身份，与“前冒险者打碎祖传酒瓶、签订劳务抵债契约”的当前剧本无关。《月台尽头的猫灯》第一次运行也曾推荐“把钱交给戴眼镜男人”，第二次重新生成开场才恢复为夜班管理员语境。

已确认 `NUMERIC_V2_ACTOR_OUTPUT_SCHEMA_INSTRUCTION` 的 `opening_phase=true` JSON 格式示例硬编码了“被中介骗、已经付房租”。模型把业务示例当成了开场候选内容；这不是 Story Package 缺少背景或身份，因为三个包的 `intro.background`、`player_identity` 和 `catgirl_identity` 均完整存在。

修复方向：

1. 输出格式示例不得携带任何现有剧本的身份、道具、场景或冲突，只保留字段形状；如果模型接口支持可靠的 JSON Schema，优先用结构化响应约束替代业务化示例；
2. 必须保留文本示例时，使用不指向具体世界观的中性占位表达，并明确禁止复制示例内容；
3. 开场 Prompt 末尾继续放置当前剧本背景、玩家身份、猫娘身份和当前开场，推荐输入必须能由这些字段直接解释；
4. 自动回归至少用三个身份完全不同的剧本生成开场，断言任一剧本的专属实体和冲突不会进入其他剧本；
5. 真实复测每个剧本至少重新生成三次开场，九次结果均无跨剧本内容后才能标记通过。

继续定向压测进一步确认该问题是稳定污染而非偶发采样：三个剧本各重复生成 4 次开场，共 12 次；其中 11 次逐字复制格式示例的两条推荐输入。排除本来就适用该内容的《云舒镇的奇遇》后，另外两个剧本共 8 次开场，有 7 次错误出现“我也是被中介骗来的，而且已经付了房租”，错误率为 87.5%。因此应优先移除业务化格式示例，再评估是否仍有身份上下文装箱问题。

修复后删除了所有带具体剧本语义的 JSON 示例，只保留字段和类型合同。真实模型分别对《云舒镇的奇遇》和《月台尽头的猫灯》生成 4 次开场，共 8/8 成功，固定“中介被骗并已付房租”跨剧本污染为 0；云舒镇推荐语继续围绕合法租客与中介失联，猫灯推荐语围绕夜班管理员和旧车站，身份上下文没有串线。

### 问题 8：场景目标已部分演出，但 Evaluator 长时间不完成并形成推荐输入循环

状态：**代码与剧本包已修复，待完整推进复测**
发现日期：2026-08-19

#### 2.21 证据与根因

《月台尽头的猫灯》第二次真实压测连续 12 回合停留在 `mainline_01`，该节点配置为 `min_turns=2`、`recommended_turns=4`。第 3—6 回合已经演出：

- 小葵取下旧雨伞并说明它来自十年前的雨夜；
- 雨伞主人曾在雨中等待整整一夜；
- 小葵将旧雨伞挂回高处。

但节点 `must_happen` 要求的是“标有特定日期的旧雨伞”。Actor 始终只说“十年前的那个雨夜”“伞骨上的日期”，没有交付可确认的具体日期；Evaluator 因证据不完整连续 12 回合返回 `scene_complete=false`。推荐输入随之在“查看日期、查询记录、询问旧伞主人”之间反复改写，软节奏预算没有把缺失证据真正交付出来。

这同时暴露 Story Package 和 Actor 收束合同的问题：剧本要求“具体日期”，却没有在节点摘要、开场或身份资料中提供可安全复用的实际日期；Actor 不能凭空编造事实，只能围绕“日期”继续追问，Evaluator 又不能把模糊指代当成已完成事实。

#### 2.22 修复方向与复测标准

1. 生成器增加“具体证据闭包”检查：`must_happen` 若要求具体日期、姓名、编号、位置或物件特征，同一节点或既有背景必须提供对应值；缺值时禁止编译或要求作者补齐；
2. 现有《月台尽头的猫灯》补充明确日期，节点摘要、目标和后续引用使用同一个事实，不让 Actor 自行创造；
3. Actor 在接近或超过 `recommended_turns` 时，只聚焦仍未完成的具体证据；第一条推荐输入必须直接促成该证据发生，不能继续生成与最近两轮语义相同的询问；
4. Evaluator 仍只判断证据和 `scene_complete`，不因为超过软预算而强制完成，也不降低作者目标；
5. Runtime 可记录连续未变化的 `pending_goals` 供 Actor 使用，但不新增 Planner、评分模型、第二次 Actor 或强制路线；
6. 自动回归覆盖“目标要求具体值但包内缺值时编译失败”和“包内有值时在推荐节奏内完成”；真实模型复测要求该幕在 3—6 回合完成，且连续两轮推荐输入不能只是同义改写。

生成器现已增加“特定日期但无实际日期”的确定性校验和局部续写回归；《月台尽头的猫灯》源项目、节点目标和安装包统一补为 `9月17日`。隔离普通回合压测中，第 1 回合已经由猫娘说明旧伞等待事实，第 4 回合明确展示“9月17日”，但本轮没有调用 Evaluator 完成整幕推进，因此仍需完整 Session 复测后关闭。

### 问题 9：严格换场合同会阻止脏提交，但 Actor 偶发缺少目标开场导致整轮失败

状态：**已修复，真实模型定向复测通过**
发现日期：2026-08-19

#### 2.23 证据、边界与修复方向

《月台尽头的猫灯》第一次真实压测在第 12 回合准备从 `mainline_02` 换场时，Actor 返回结果没有满足目标开场前缀合同，抛出：

`numeric_v2_actor_target_opening_missing`

本次失败没有提交 Session、Ledger 或半回合，玩家输入可以按现有前端合同回退到输入框，证明原子保护正确；但玩家仍会经历一次长时间等待后发送失败。第二次运行没有复现该异常，却因问题 8 停在第一幕，说明这是一项随机可靠性风险，不能因单次复测未复现就关闭。

修复不能退回“解析器放宽后照常提交”，也不增加多轮 Repair。优先方向是：

1. 目标 `opening_scene` 由 Runtime 作为确定性 `target_opening.scene_narration` 前缀写入最终 performance，Actor 只生成来源回应、过渡桥、目标幕补充演绎和对白；
2. Actor 不再负责逐字复述 Runtime 已知的目标开场，从结构上消除“模型少一个字导致整轮失败”；
3. Runtime 合并后仍校验三段顺序、目标节点和非空猫娘对白，原子提交边界不变；
4. 保留现有输入回退作为最终失败体验，不把失败结果静默转换为成功；
5. 使用同一换场点至少运行 20 次，目标开场均由 Runtime 精确交付且没有半回合后，才能标记通过。

继续定向压测固定《月台尽头的猫灯》`mainline_02 → mainline_03` 的同一个 Session 状态和同一句玩家输入，真实调用 Actor 12 次：5 次成功、7 次抛出 `numeric_v2_actor_target_opening_missing`，失败率为 58.3%，平均每次等待约 4.9 秒。失败前已经消耗完整模型等待时间，进一步证明输入回退只能保护数据，不能解决交互可靠性；目标开场必须从模型逐字复述职责中移出。

修复后，Actor 不再生成或复写目标 `scene_narration`，Runtime 根据已选目标节点确定性注入开场原文。复测同时发现模型会把三段 phase 写成 `response / transition / opening`、`transition / bridge / opening` 或 `normal / transition_bridge / opening`；解析器现只用固定位置和互斥字段形状确定段位，再规范化 phase，仍拒绝缺段、错序和错误字段。最终同一换场点连续 20/20 次真实调用成功，目标开场 20/20 与剧本原文完全一致，无 Repair、半回合或正式 Session 写入。

### 问题 10：普通回合文本和单幕交互仍偏长，推荐输入出现局部复读

状态：**复测未通过，待继续修复**
发现日期：2026-08-19

#### 2.24 证据与修复方向

第 6 轮真实压测的普通回合表现如下：

| 剧本 | 成功回合 | 换场 | 普通回合最长可见文本 | 单个普通微动作最长 | 主要体验 |
| --- | ---: | ---: | ---: | ---: | --- |
| 云舒镇的奇遇 | 8 | 2 | 78 字 | 24 字 | 节奏相对紧凑 |
| 猫尾特调恋爱指南 | 10 | 2 | 147 字 | 28 字 | 4 个普通回合超过 100 字，早餐主题连续停留 6 回合 |
| 月台尽头的猫灯（复测） | 12 | 0 | 94 字 | 26 字 | 旧伞、日期和记录持续循环 |

普通微动作均超过架构文档“目标不超过 18 个汉字”的软目标。《猫尾特调恋爱指南》在早餐幕反复推荐“早餐、牛奶、调料、尝尝”，即使最终完成换场，玩家也会感到同一互动被拉长。

修复方向：

1. 普通回合与开场/换场使用不同输出预算；普通回合收紧模型最大输出，换场三段继续保留完整叙事空间；
2. Prompt 明确普通回合总量目标、单个微动作目标和动作数量上限，避免用三组长动作重复解释同一种情绪；
3. 最近两轮推荐输入按完整句进入 Actor 上下文，要求新候选推动新的可观察事件，不只替换同义词；
4. 不用前端截断、隐藏后半段或删除对白来伪造简短，也不把轻微超长直接变成整轮失败；
5. 回归统计普通回合可见字符、微动作长度、连续主题重复和响应耗时；真实复测目标是 90% 普通回合不超过 100 字、单个微动作不超过 24 字，且达到 `recommended_turns` 后不再连续两轮围绕同一未变化动作打转。

Actor 已把普通回合目标收紧为 40—100 个中文字符、1—3 句对白和 1—2 个微动作，并把最近两轮完整推荐语作为“只用于避重复、不是已发生事实”的输入。两个剧本各连续 4 个普通回合，共 8/8 成功，可见正文为 41—90 字；猫灯按“旧伞来源 → 旧物边界 → 取下旧伞 → 展示日期”推进。云舒镇第 3—4 回合仍重复“把行李放到地毯”及近义推荐语，说明语义复读尚未完全消失；《猫尾特调恋爱指南》也未在本次修复后重跑，暂不标记完全通过。

三剧本完整链路复测后，《云舒镇的奇遇》和《月台尽头的猫灯》普通回合最长分别为 76 字和 65 字，符合目标；《猫尾特调恋爱指南》仍出现 144 字普通回合，早餐幕用了 5 回合才从 `mainline_02` 推进到 `mainline_03`。第 4—8 回合持续围绕“尝一口、再尝一口、浓汤、配菜、以后做早餐”打转，最近推荐语虽没有达到 0.8 的字面近重复阈值，但互动语义没有产生足够变化。问题因此不能按先前 8 个短样本关闭。

当前产品决定是不把 100 字作为硬验收线，单轮略微超过可以接受。本轮没有修改任何普通回合字数、句数或微动作长度约束；最新三剧本完整链路分别用 9、8、8 回合到达目标节点，语义复读显著下降。后续只把持续围绕同一事实打转或明显拖慢推进视为失败，不因少量超字单独回退。

### 问题 11：剧本包事实文本没有完全同步，换场出现早餐与晚餐矛盾

状态：**已修复，真实模型完整链路复测通过**
发现日期：2026-08-19

#### 2.25 证据与修复方向

《云舒镇的奇遇》当前安装包仍在节点摘要和过渡合同中保存“抢救出来的一条冷冻秋刀鱼”，没有完全落实此前确定的“一条冷冻秋刀鱼”。真实演绎从“合租第二天清晨、简单早餐”开始，下一次换场桥却总结为“这顿晚餐”，同一次可见演绎内出现时间矛盾。

修复应回到生成器项目源数据统一修改，再重新编译、校验并原子替换安装包；不能只修改已安装 JSON 或让 Actor 猜测餐次。编译前增加同节点时间词一致性检查，至少检查开场、摘要、`must_happen`、过渡合同和目标开场中的“清晨/早餐/傍晚/晚餐”等明显冲突。更新后用新隔离 Session 重跑前两幕，确认秋刀鱼名称和餐次全程一致。

《云舒镇的奇遇》生成器源项目现已统一为“合租第二天傍晚”和“一条冷冻秋刀鱼”，随后重新编译、通过 N.E.K.O 校验并原子替换安装包；唯一已结束 Session 只同步新包哈希，历史演绎未改。尚未用 Evaluator 从第一幕完整推进到第二幕，因此保留餐次连续性复测项。

完整链路复测用 5 回合从 `mainline_01` 推进到 `mainline_03`。进入第二幕时 Runtime 可见开场为“合租第二天傍晚”，后续始终使用“晚餐、烤秋刀鱼、味噌汤”和“一条冷冻秋刀鱼”，没有再出现早餐、清晨或“抢救出来”的旧文本；该事实同步问题已闭环。

### 问题 12：目标节点摘要预设玩家行动，换场时把未发生决定写成事实

状态：**已修复，当前包与支线真实复测通过**
发现日期：2026-08-19

#### 2.26 证据、根因与修复方向

《月台尽头的猫灯》`mainline_03` 的 `story_beat.summary` 以这句话开头：

`为了验证日志上的线索，你提议在午夜后巡视站台。`

该节点同时声明 `must_not_happen` 包含“不让玩家被强制替代做决定”，两者直接冲突。定向换场压测的玩家实际输入只是“我把日志残页整理好，和你一起继续寻找下一条线索”，没有选择午夜、巡视或站台。5 次成功换场均把“你提议在午夜后巡视站台”作为目标开场展示，等同于替玩家补写关键行动。

根因在 Story Package 和目标开场投影，不在 Actor 自由发挥：当前目标 `opening_scene` 从节点摘要开头确定性取得，而摘要本身用第二人称预演了玩家决定；严格目标开场合同又要求 Actor 原样复述该文本。

修复方向：

1. 生成器要求每个节点摘要的开场句只建立时间、地点、环境和猫娘可见状态，不得断言玩家已经提议、同意、拒绝、移动、触碰或作出选择；
2. 编译器增加玩家归属检查，目标开场候选中出现“你/玩家/男主/哥哥 + 主动行为”时阻止编译并要求作者改写；不得由 Runtime 在安装后猜测删除句子；
3. 现有《月台尽头的猫灯》把开场改为不预设玩家决定的站台环境，例如先建立午夜、末班车和信号灯状态，再由猫娘对白邀请玩家调查；
4. Runtime 确定性注入目标开场后，仍需校验该开场来自已通过玩家归属检查的包；Actor 只能回应玩家本轮已经表达的行动范围；
5. 自动回归覆盖“玩家只同意继续调查，但目标摘要预写具体时间地点”的反例；真实复测使用拒绝、暂缓和改变调查方式三种输入，均不得被换场文本改写为已同意午夜巡视。

生成器现会检查章节首句任意位置的“男主/玩家/你/他 + 提议、决定、进入、巡视、调查等主动行为”，失败时只续写对应 `narrative`。《月台尽头的猫灯》目标摘要已改为“最后一班慢车驶离后，整个车站陷入死寂”，不再预写玩家提议；Actor 的开场投影还会在旧包摘要首句不安全时选择后续可观察场景句。最终 20 次同状态换场均只展示环境开场，没有再写入“你提议午夜巡视”。

完整链路复测证明修复范围仍不完整：云舒镇纸箱幕确定性交付“哥哥开始清理堆积如山的快递纸箱”；猫灯第二幕桥接写“你埋首于遗失物中，分类、登记、打包”；鸡尾酒早餐幕写“哥哥系着围裙，熟练地处理食材”，改革支线又写“哥哥展示了一份详尽的商业改革计划书”。这些都不是玩家当轮已经执行的动作。当前生成器校验只覆盖新生成主线的首句，尚未覆盖已有项目迁移、支线生成、过渡桥中的玩家行为和包发布前全图审计，因此问题 12 重新打开。

修复后 N.E.K.O 编译器会阻止节点首句、`must_happen` 和 `transition_contract.must_deliver` 预写玩家行动，也会阻止用“女主强迫/逼迫/命令男主完成行动”绕过主体检查；生成器主线与终点先行支线使用相同约束。三个现有项目已把开场、支线和过渡改为环境或女主主动交付，并在保持节点、路线、数值条件不变的前提下原子替换安装包。最新 8 次真实换场没有再替玩家清理纸箱、展示计划书、进入车厢或作出去留决定。

### 问题 13：推荐输入仍会退化成操作说明或机械第一人称动作

状态：**已修复，真实模型定向复测通过**

#### 2.27 证据与修复方向

《云舒镇的奇遇》最新开场返回：

- `举起双手表示没有恶意，轻声解释自己是新租客`
- `展示手中被雨水打湿的合同，无奈地苦笑`
- `注意到她警惕的眼神，慢慢后退一步保持距离`

这些内容可以描述玩家意图，却不是点击后可原样发送的台词或动作，与既定“推荐选项直接写说什么、做什么”的交互合同冲突。此前移除业务示例解决了跨剧本污染，但没有保证输出语态；当时的 `_suggestions` 只检查数组、长度和去重，无法区分直接输入与“解释、展示、注意到”等操作说明。

第一次修复通过 Prompt 和输出过滤消除了操作说明，但最新《云舒镇的奇遇》演绎又返回“我把湿漉漉的合同递过去，上面确实有我的名字”。根因是 Actor 的通用合同仍明确要求“第一人称台词或动作”，并使用“我先……”作结构示意，导致模型把动作机械写成“我……”叙述，同时没有用引号区分动作后的实际台词。

Actor Prompt 现明确推荐语不强制以“我”开头：纯动作省略玩家主语并从动作动词起笔；纯台词直接写实际说出口的话；动作与台词混合时用中文引号标出台词；只有对白内部才按语义自然使用“我”等代词。输出解析器继续确定性丢弃以“解释、询问、表示、保证、提出、展示、选择、注意到、观察、尝试、请求、说明”等开头的编辑指令，以及“问她是否/为什么”等间接问题摘要；动作后再接“表示没有恶意、解释身份、说明来意”等意图摘要也会被丢弃。模型返回的成对英文双引号在提交前统一规范为中文引号，不修改对白内部自然使用的“我”。

第 11 轮首次压测生成 3 个开场、16 个完整回合和 3 次换场，共 59 条推荐输入；没有出现“我把……”“我拿……”式机械动作。修复后再次定向运行《猫尾特调恋爱指南》，开场及 4 个回合的推荐台词全部使用中文引号，动作项均从动作动词起笔，操作说明与意图摘要命中数为 0。

### 问题 14：支线文本泄漏作者候选名，角色投影并未覆盖全图

状态：**当前三个项目已修复并实测通过，泛化拦截继续观察**

#### 2.28 证据与修复方向

《猫尾特调恋爱指南》从 `mainline_03` 进入 `node_branch_44a330dc73` 时，可见场景旁白出现：

`哥哥在打烊后向米娅展示了一份详尽的商业改革计划书。`

当前猫娘是小葵，正式角色槽位只能使用“女主/当前猫娘”；“米娅”是作者候选名。主线身份投影回归仍通过，但支线摘要在生成和编译时保留了候选名，而 `NumericV2CastProjection` 又无法仅凭已归一化的主线身份推断所有支线旧名，最终导致可见泄漏。

修复应在生成器支线 Prompt、支线草稿应用、编译和发布前全图校验中统一禁止候选男女主姓名，只允许“女主/男主”固定槽位；现有三个项目也要扫描全部节点、过渡合同和结局，不能只依赖 Runtime 临时替换已知名字。自动回归需要在支线摘要、`must_happen`、回接合同和独立结局中分别注入候选名并阻止发布。

生成器支线 Prompt 现只允许“女主/男主”角色槽位，现有三个项目的节点、过渡和结局也已清除候选名。真实模型再次进入《猫尾特调恋爱指南》改革支线，当前猫娘始终显示为小葵，没有再出现“米娅”；《月台尽头的猫灯》也没有擅自补出未定义的爱人姓名。由于任意中文专名无法在缺少候选名清单时完全静态识别，后续新生成项目仍需保留发布前全图扫描。

### 问题 15：Evaluator 会把玩家提供的目标答案视为猫娘事件已经完成

状态：**已修复，完整链路复测通过**

#### 2.29 证据与修复方向

猫灯第一幕要求“女主将伞柄内侧刻着9月17日的旧雨伞挂回高处，并说明主人等待一夜”。复测中：

- 玩家第 1 回合主动问“是不是刻着9月17日”；
- 猫娘只说伞主人等待一夜，没有展示或复述 `9月17日`；
- 第 2 回合猫娘只说“十年前的雨夜”；
- 第 3 回合猫娘把伞挂回并介绍主人，仍未说出日期；
- Evaluator 却返回 `scene_complete=true`，Runtime 随即换幕。

`9月17日` 直到下一幕开场才由猫娘说出。说明 Evaluator 把玩家输入中的目标答案与猫娘部分动作合并成了完成证据，没有严格遵守 `must_happen` 的主体归属。由于 Evaluator 在 Actor 之前执行，后续应明确：玩家当轮输入只能完成玩家所有的目标；“女主/环境”目标必须由已提交的猫娘表现或环境旁白证明，不能用玩家请求、猜测或复述替代。允许因此多停留一回合，但不能提前选择路线。

Evaluator 输入现为每个目标附带 `owner`，猫娘目标只能由已提交的猫娘对白、动作或场景旁白证明；玩家请求、猜测或复述目标答案不能补齐日期。最新完整链路中，玩家首轮仍主动说出 `9月17日`，猫娘随后亲口确认日期并说明等待一夜的事实，第一幕在第 3 回合完成，而不是仅凭玩家答案提前换场。该剧本后续两幕分别用 2/3 回合进入隐藏支线，没有再形成 10—12 回合循环。

新生成的《猫尾特调恋爱指南》进一步暴露了复合目标反例：玩家先提出“试一个月、一个实验区、保留原经营方式”，猫娘只说“就这么定”，Evaluator 仍曾把三项全部判为完成。修复后，生成器要求多个独立条件拆成多个 `must_happen`；作者还可用引号标出必须由指定主体直接交付的硬证据。Evaluator 会按目标 owner 确定性检查引号内容，猫娘硬台词没有真实出现在猫娘对白时把完成信号降为 `false`，保留本轮数值并继续当前幕。Actor 同时按 `recent_context` 跳过已交付硬台词，普通节奏每轮最多新交付一项。最终真实链路按“一个实验区 → 一个月 → 保留原经营方式”分三轮交付，第 4 个支线回合完成并回接主线。

第 11 轮又发现另一种复合目标反例：猫尾特调第一幕把三个月、岗位、每日八小时、每周休息两天和实际损失赔偿写在同一个“明确说明：……”目标中。猫娘只交付部分条款时，模型仍曾返回 `scene_complete=true`。Evaluator 现会从这类明确说明目标中确定性提取期限、数量、频率、岗位名和赔偿依据等作者字面锚点，只在猫娘已提交对白中逐项核对；玩家输入、推荐语和模糊确认都不能补齐缺项。定向复测中五项条款分别在第 2—3 回合完整交付，第 4 回合签约后才进入下一幕，没有再提前推进。

### 问题 16：换场桥与确定性目标开场重复，单次可见换场过长

状态：**确定性重复已修复，语义长度继续观察**

#### 2.30 证据与修复方向

本轮 8 次换场都成功且没有半回合，但可见换场正文为 165—293 字。常见结构是 Actor 的 `transition_bridge` 已完整描述“末班车驶离、车站陷入寂静”或“转眼到合租次日傍晚”，紧接着 Runtime 又逐字注入同义的目标 `opening_scene`，玩家连续看到两遍相同时间、地点和环境变化。

目标开场必须继续由 Runtime 确定性交付，不能为了缩短而重新交给模型。Actor 的桥接职责应收窄为来源场景收束与必要时间/行动连接，并明确禁止复述目标开场已经提供的环境事实；体验统计应分别计算来源回应、桥接、目标开场和目标对白，不能用普通回合 100 字目标直接限制完整换场。

Actor 不再接收目标节点的 `opening_scene` 作为桥段素材，过渡合同也会排除由 Runtime 开场覆盖的交付项；输出解析器按完整句移除与目标开场重复度过高的桥接句，Runtime 最后仍原子注入唯一目标开场。最新正式《云舒镇的奇遇》第 12、16 回合暴露去重后使用固定句“时间向前流转，现场随之转换”的新问题：该系统式占位连续出现且比直接换场更突兀。现已删除默认句；桥段去重后没有独立事实时保留内部三段式提交结构，但 `scene_narration` 为空且前端直接展示目标开场。有真实来源收束或必要时间、地点变化时仍显示桥段。对已经提交该旧占位句的 Session，前端只在 `transition_bridge` 中精确隐藏该原文，不改写 Session 或 Ledger。桥段与目标场景允许存在必要的时间连续性，且按当前产品决定不设置严格总字数上限；只有重复同一环境事实或明显拖慢阅读时继续记录为体验问题。

### 问题 17：换场来源回应复读上一轮，目标场景掩盖整轮近重复

状态：**已修复，真实模型定向复测通过**

#### 2.31 证据与修复方向

第 11 轮《猫尾特调恋爱指南》第 5 回合已经说“既然签了字，那现在就是正式员工啦”“先去厨房熟悉一下环境”，第 6 回合换场的 `source_response` 又逐字重复两句。既有近重复保护比较整个换场正文，后续桥段和目标开场拉低了整体相似度，因此无法识别来源段复读。

Actor 提交前现会单独把 `source_response.performance` 与上一轮完整表现比较；全部对白照搬且来源段整体近似时拒绝提交，Session、Ledger 和 revision 保持不变，前端继续使用既有输入回退。普通回合的整轮近重复规则不变。修复后的同一剧本定向复测在第 4 回合换场，来源回应改为新的简短承接，没有重复上一轮动作或对白。

### 问题 18：隐藏关系仍在最低阶段，可见演绎已经提前进入粘人状态

状态：**Actor 合同、数值结算和未来生成默认已修复，真实模型定向复测通过；完整前端关系弧待验收**

#### 2.32 证据与修复方向

最新正式《云舒镇的奇遇》Session 在第 10 回合仍处于好感 `礼貌疏离` band，但猫娘已连续使用“麻烦哥哥了”“人家会乖乖等着的”“等着吃鱼了哦”等明显依赖式表达。当前节点实际已经停留 4 回合，上一节点停留 6 回合，因此根因不是 Runtime 提前越过 `min_turns`，而是 Actor 只拿到 band 文案，没有被明确要求把它作为可见亲密度上限；角色卡中的甜美、温柔、粘人又被模型误解成当前关系已经建立。开场警惕一度被演成威胁，也放大了随后软化的突变感。

关系型 metric 现只由 Story Package 显式 `relationship_effect` 识别，正向和反向数值统一投影为 `stranger / guarded / cooperative / trusted / intimate`，多个关系数值再与当前幕关系上限取最严格者。最低阶段只允许基本礼貌、有限软化和保持距离，不得主动暧昧、占有、依赖或使用伴侣式亲昵称呼；较高阶段仍须由已发生事实支撑。核心人格继续决定表达方式，不能用于越过关系阶段，低关系也不授权敌视、羞辱或威胁。

当前回合 Actor 使用结算前关系数值，跨 band 后从下一回合才改变可见关系，避免一次行动触发瞬时亲密跳变；非关系能力状态仍可使用结算后的候选数值。Evaluator 不再直接返回任意 delta，只选择作者 criterion 和 `weak / normal / strong / decisive`，服务端按单回合限幅确定性换算；最近四条 Ledger 已奖励过同一关系依据时，重复礼貌、换一种说法或延续同一态度不能继续刷值，除非出现新成本、风险、兑现或可验证结果。

关系语义不再使用“乖乖听话”“都听你的”、抓拉、贴近等有限关键词正则做提交前硬校验。此类校验无法覆盖同义改写，并会把自然配合误判成关系越级；同一次 Actor 自报检查也在真实探针中出现过正文写“顺从地垂落”、检查却声明无依赖的矛盾，不能作为提交依据。Runtime 现按 `effective_stage` 投影阶段化 `response_contract`，在核心人格之后最后收口本轮正文与推荐输入的允许姿态；生成器同时把未来主线和支线默认节奏调整为 `min_turns: 8 / recommended_turns: 15`，关系型数值默认单回合增减上限为 3，非关系型保持 5，并要求亲密关系主线按关系弧逐幕推进。

真实模型定向复测使用新 Story ID、新 Session 和当前小葵角色卡连续演绎 10 回合：第一幕在第 8 回合才换场，关系数值最终为 2，仍处于 `guarded`；“观察、检查安全”等泛化行为没有误加黑客能力。换场在 4800 Token Actor 输入预算内完成。初次压力探针仍捕获了抓衣袖、主动靠近和摸头推荐语；改为阶段化响应合同并调整上下文优先级后，相同低关系输入连续 5 次真实 Actor 抽测均保持静止、自检、观察或追问，没有再出现讨好式服从。三处支线阈值状态的真实 Actor 换场探针均成功生成合法三段式过渡，并分别保持各自计算出的关系上限。以上证明当前单次生成合同明显改善低关系表现，但在不增加语义分类模型或 Repair 的边界下不宣称百分之百硬拦截，仍需真实前端从低关系连续演到高关系的完整体验验收。

### 问题 19：称呼在 Session 初始未知前被 Actor 提前使用

状态：**代码已修复待真实前端复测**

#### 2.33 证据与根因

旧链路把配置中的玩家昵称无条件写入猫娘绑定，并在 opening、普通回合和换场 Prompt 中直接告诉 Actor。即使剧本语义上要求开场不知道称呼，真实模型仍可能在第一回合提前叫出昵称；自然语言关键词拦截无法覆盖同义表达，也不能安全推断“称呼未知”。

#### 2.34 解决方案与回归标准

- Story Package 的 `initial_state` 增加结构化 `player_address_known` 布尔值；旧包缺少该字段时只按 `false` 兼容；不从剧情文案解析称呼状态；
- Session 持久化同名状态，重新开始按 Story Package 初始值重置，继续恢复已提交值；旧 Session 缺少字段时仅检查已提交玩家输入中是否出现完整配置昵称；
- 未知阶段 Actor 的所有稳定身份投影统一使用“你”，不接收真实配置昵称；用户本轮精确包含完整昵称时，只把它作为披露候选；
- Runtime 在 Actor 成功后把候选状态与 Ledger、Session、表现历史一起原子提交；Actor 或 Store 失败不能提前修改；
- 未知阶段对 Actor 正文和推荐输入做完整昵称精确字符串保护，不使用语义正则猜测；
- 节点可声明结构化 `story_beat.acting_contract`，明确开机认知、记忆、自称和人格权限；`system_neutral` 阶段隐藏角色卡显式自称事实，违规输出阻止提交，不改写模型正文；
- 自动回归必须覆盖开场、普通回合、换场、失败不落盘、继续恢复和重新开始重置；真实前端复测需用新 Session 验证四类流程，并确认未披露前的 Prompt 与输出不出现配置昵称。

### 问题 20：换场上下文重复且过渡桥会替玩家补写行动

状态：**代码已修复，真实模型定向复测通过；完整前端长程演绎待验收**

#### 2.35 证据与根因

为同时保住来源回应和目标开场，旧换场 Prompt 叠加了完整来源 beat、完整目标 beat、目标自由文本临时状态、两套数值解释、风格说明和历史记录。当前《霓虹下的猫耳重构》实际换场输入因此一度超过 4800 Token；角色卡整段进入 `style_only` 合同时还会夹带关系定位、特殊自称和昵称等非风格事实。压缩后的首轮真实模型探针虽然均返回合法三段结构，但 2/3 的 `transition_bridge` 仍把“切断线路、带她离开”等行动直接分配给用户，说明只靠提示词声明玩家归属仍不足以成为提交不变量。

#### 2.36 解决方案与回归结果

- 换场在通用预算装箱前使用确定性紧凑投影：保留稳定剧情前提、结构化称呼状态、来源硬边界、目标待完成目标与硬边界、Runtime 目标开场、过渡合同、最近完整历史、两幕演绎/关系合同和本轮输入；删除已消费来源目标、目标自由文本临时状态以及重复的能力、优先级和风格说明；
- `persona_scope=style_only` 只允许结构化标注为性格、行为、喜好、厌恶、口癖、说话风格或语气的角色卡字段进入 Actor，不再携带昵称、特殊自称、关系定位或未标注关系记忆；
- 复合“男女主”身份槽先整体投影为“你和当前猫娘”，再处理单一角色名，避免出现“男小葵”等部分替换；
- `transition_bridge` 只允许环境、时间和地点变化；提交前对“你 / 您 / 男主 / 玩家 / 配置昵称”做精确主体保护，命中即整回合失败，不调用第二次 Actor，也不提交 Session；
- 当前正式剧本换场 Prompt 从约 5743 Token 降至 4018 Token。增加保护后的真实模型连续 3 次换场探针均返回环境型或空桥段，没有再替用户行动；Numeric v2 自动回归 149 项、前端与浏览器回归 27 项通过。

### 问题 21：确定性核心合理，但自由文本合同与边界模块承担了过多职责

状态：**代码收敛与自动回归完成；真实模型长程演绎待复测**

#### 2.37 全流程审查结论

本轮按“选择剧本 → 开始或重新开始 → 开场 → 普通回合 → Evaluator 判定 → Runtime 路线结算 → Actor 演绎 → 原子提交 → 换场 → 继续恢复 → 主动结束 → 记忆归档或跳过 → 删除剧本与恢复审计”的完整链路检查代码、自动回归和桌面窗口冒烟。结论不是“Numeric v2 整体过度设计”，而是复杂度分布不均：

- Session、Ledger、完整重放校验、revision、幂等键、剧本级提交锁和失败不落盘共同保证确定性与原子性，均有并发、响应丢失或损坏恢复场景支撑，不应删除；
- 结束回执、暂存公开冷档案、记忆服务幂等键和恢复事务用于处理跨服务未知响应，不是可以用一次普通 HTTP 请求替代的多余步骤；
- BroadcastChannel、`window.opener` 直连消息和本地活动指针分别服务独立窗口同步、启动握手和应用重启恢复，不能只保留其中一个；
- 真正需要收敛的部分，是 Story Package 仍用自由文本同时表达可见开场、关系上限、目标归属和证据要求，迫使 Actor 与 Evaluator 使用正则、前缀、引号和文本相似度恢复结构；
- `numeric_v2_actor.py` 同时负责角色投影、历史装箱、预算、Prompt、模型调用、输出解析和提交前保护；Router 同时负责 HTTP 校验、回合工作流、TTS、归档与记忆写入。两者边界过宽，继续追加规则会增加回归面；
- 当前全量 Session 重放和每次返回完整表现历史是可观察的线性成本，但现有剧本回合规模尚未证明它是性能瓶颈。在真实长 Session 压测前，不增加缓存、增量日志或前端差量协议。

#### 2.38 修复顺序

1. **先结构化作者事实**：Story beat 增加明确的 `opening_scene`、`relationship_ceiling` 和结构化目标合同；新包优先使用结构化字段，旧包继续走现有兼容投影，不能通过编译默认值改变旧包哈希或让旧 Session 失效。
2. **再收窄 Actor 与 Router**：换场直接构造紧凑数据，不先组装完整上下文再删除；Actor 只保留单次模型编排，把 Prompt/预算与输出解析/保护拆到边界清晰的辅助模块；Router 只保留请求校验和响应映射，把回合提交与归档事务抽成应用工作流。不得增加 Planner、Repair、第二次 Actor 或额外评分模型。
3. **最后做低风险清理**：合并两份前端请求与跨页消息公共代码；剧本列表复用首次编译结果，避免同一包重复读盘和编译；收敛仅测试使用的重复重启入口；为 Session、剧本、回执、TTS 和归档请求锁建立可回收的生命周期，避免长时间运行时锁表只增不减。
4. **保留观察项**：只有真实长 Session 数据证明重放、快照大小或前端重建成为瓶颈后，才考虑缓存或增量协议；优化前仍以可恢复、可审计和响应丢失后可重建为硬条件。

#### 2.39 验收边界

- 新结构化字段必须有编译错误路径、旧包兼容和 Actor/Evaluator 消费回归；
- 重构前后模型调用次数保持为每回合一次 Evaluator、一次 Actor，Runtime 路线与提交结果一致；
- 响应丢失、重复提交、并发 revision、继续、重新开始、结束归档、跳过归档、删除恢复和 Electron 子窗口仍需通过原有测试；
- TTS 音频质量继续由独立修复负责，本轮只保护已提交对白块的调用边界，不以设备播放结果作为重构验收条件。

#### 2.40 实施结果

- Story beat 已支持结构化 `opening_scene`、`relationship_ceiling` 和 `goals`；目标明确保存稳定 ID、`owner` 以及 `semantic / exact` 证据方式。新字段由编译器严格校验，旧包仍在消费边界兼容投影，编译器不会把缺省字段写回旧包或改变旧 package hash；
- Actor 普通回合与换场在入口处分流。换场直接构造紧凑上下文，不再先创建完整普通上下文再做删除；输出解析、混合正文校验、换场桥去重和推荐输入保护已经拆入 `numeric_v2_actor_output.py`，仍然只有一次 Actor 调用；
- Router 的普通回合端点只负责 HTTP 校验和错误映射，固定的 Evaluator → Runtime 候选 → Actor → 猫娘身份复验 → 故事保护 → 原子提交顺序已收进 `numeric_v2_workflow.py`，没有增加模型、重试或新业务状态；
- 剧本列表直接复用注册表第一次编译得到的 `intro`，不再为显示简介逐包二次读取和编译；生产重开统一使用显式旧 Session ID 的 `replace_active_session` 原子替换，删除仅测试使用的模糊重启入口和无调用 Store 覆盖接口；
- Session 文件锁、故事锁、结束回执锁、TTS 请求锁和归档请求锁改为调用期间强持有、空闲后弱引用回收；新增回归同时验证并发窗口内复用同一把锁，以及释放最后强引用后不保留历史 ID；
- 选择页和本体运行时只共享消息协议号、请求 ID、CSRF JSON 请求和消息信封；各自的窗口目标、活动状态、启动握手、结束流程和恢复指针仍保持独立，避免把协议去重扩大成新的前端业务框架；
- 全量 Session 重放、完整权威快照、结束回执、删除事务和三种跨窗口恢复信号保持不变。它们仍有损坏恢复、响应丢失、并发或应用重启场景支撑，不属于本轮删除范围。
- 最终自动验证为 Numeric v2 单元与前端回归 191 项通过、Electron 主页面与真实子窗口冒烟 3 项通过；当前安装的《月影森林的四叶契约》和《霓虹下的猫耳重构》均能由新编译器通过，且编译所得 package hash 与各自既有 Session 绑定保持一致。尚未把这次边界重构等同于演绎质量通过，仍需用新 Session 进行真实模型长程复测。

### 问题 22：推荐输入的发散、收束与关键物品事实边界混在一起

状态：**代码与剧本包已修复，真实模型定向复测达到可用；单 Actor 概率残余保留**

#### 2.41 产品边界与根因

推荐输入不是隐藏 Planner。达到 `min_turns` 前，它应提供方向不同的未来候选，允许探索、试探和非关键细节即兴；达到 `min_turns` 且仍有 `pending_goals` 后，每回合至少提供一条可直接靠向下一幕的候选；达到 `recommended_turns` 后才把全部候选收束到剩余主线目标。玩家始终可以忽略候选继续自由输入，真正的目标完成、节点推进、路线和换幕仍只由 Evaluator 与 Runtime 决定。

旧合同混淆了三件事：接近建议回合数时过早把推荐输入绑到主线；把尚未点击的推荐候选当成已发生事实约束，压掉合理发散；又没有把“普通即兴细节”和“会推动目标、路线或结局的关键物品事实”分开。真实探针因此一度在月影森林中替修复工具补出银色、发热和稳定魔力等属性，也会把 `owner=catgirl` 的提问原样写成男主推荐输入。结构化目标和关系结论重复展开还曾让收束态固定上下文超过 4800 Token。

#### 2.42 解决方案

- `normal` 与提前一回合的 `focus` 均投影 `suggestion_instruction.mode=diverge`；`focus` 只让正文轻柔靠向本幕目标，不强制推荐语提前命中主线；
- 达到 `min_turns` 且仍有未完成目标时使用 `mainline_pull`，每回合第一条候选直达剩余目标；达到 `recommended_turns` 后改为 `mainline_closure`，全部候选服务剩余目标，但仍不得催迫玩家决定或编造外部截止时间；
- 收束态按结构化目标 `owner` 分配演绎职责：猫娘动作或提问由 `performance` 完成，第一条推荐只让男主回应或披露；环境目标由正文交付、推荐负责回应；玩家或共享目标不由 Actor 代做，只作为可选输入；
- 身份、记忆、关系、玩家行动和已发生事件继续严格承接正式事实；不影响目标、路线或结局的氛围与非关键细节允许合理即兴；
- 被 `pending_goals` 或 `acting_contract` 锁定的关键物品，其未确认外观、状态、名称、用途、能力、效果、反应和来历在正文与推荐输入中都保持未知。允许询问、检查或提议不预设结果的试验，不能直接补出答案；
- 《月影森林的四叶契约》把本幕关键工具明确为掌心大小、灰银色、无铭文、冷却无光的闭合圆环，并只保护该物品；普通工具和环境细节继续允许即兴。《霓虹下的猫耳重构》不把雨声的技术化描写列为失败，曾加入的雨声限制已撤回；
- Actor 对新结构化包不再重复发送 `goal_progress`，关系上下文只投影最终阶段和 `response_contract`。没有增加 Planner、Repair、第二次 Actor、额外评分模型或语义关键词正则。

#### 2.43 验证结果与剩余边界

- 正式月影森林包为 `draft_11`，hash 为 `sha256:b5411db7cf4b56fe2d1deff13329d7ded7eb5e831d62f8381bce0607e8f26478`；霓虹包保持 `draft_11`，hash 为 `sha256:82ef51f8c9a223f02d2bbb58c832204ad0e2ae8d5456e5226b478fce68166d7b`。两包编译均为零 warning，既有正式 Session 同步后均可恢复；
- 收束态实际提示词为月影森林 4723 Token、霓虹 4378 Token，均低于 4800 Token；定向 Actor/合同回归 112 项通过，Numeric v2 单元、Router、页面与浏览器相关回归 200 项通过，Electron 主应用 smoke 2 项通过；
- 修复前月影森林 3 次探针有 2 次替关键工具补造外形、发热或功能；收紧关键物品合同后连续 5/5 未再补造名称、功能、效果或异常反应，且第一条推荐 5/5 回到圆环；
- 霓虹收束态在目标主体规则加强后，5 次中 4 次由猫娘在正文询问地点或身份，第一条推荐让男主回答；1 次仍把猫娘应问的问题倒置为男主推荐输入。该残余属于单次自由文本生成概率，不影响 Runtime 的确定性推进，也不宣称百分之百硬拦截；若未来要求确定性消除，只能另行评估结构化候选或额外校验，不能在本轮偷偷加入第二次模型调用。

### 问题 23：回合节奏语义、完成状态和校园剧本合同没有形成同一闭环

状态：**通用代码与《毕业铃响前的半步》剧本包已修复，自动回归和真实模型定向压测通过；正式前端人工长程演绎待验收**

#### 2.44 根因与产品语义

此前把 `recommended_turns` 误解成“开始拉回主线”的时点，导致 `min_turns=8 / recommended_turns=15` 的剧本在第 8—14 回合仍可能只提供发散候选。正确语义是：第 1—6 回合允许自由发散，第 7 回合正文轻柔聚焦，第 8 回合起每回合至少保留一个主线入口；玩家若始终忽略推荐，仍可继续自由输入，到第 15 回合后才进入更强但不强迫的收束。

同一轮检查还发现三处会拖长演绎的问题：结构化目标证据已经全部成立时，Evaluator 仍可能返回 `scene_complete=false`；Runtime 已锁存完成状态后，Actor 仍可能再次看到整组目标；完全相同的玩家输入在旧四回合冷却结束后，还能反复命中同一数值 criterion。《毕业铃响前的半步》本身又把私人手账与学校记忆展资料写得不够分明，并把“共同选择暂不定义关系”演成女主受伤后的服从。

#### 2.45 通用修复

- Actor 统一使用 `normal → focus → guided → closure` 四阶段：以 `min_turns=8 / recommended_turns=15` 为例，第 7 回合轻柔聚焦，第 8—14 回合每回合至少一条 `mainline_pull`，第 15 回合起全部候选使用 `mainline_closure`；
- `guided` 和 `closure` 都先回应玩家当前自由输入，再推进尚未完成目标。推荐输入始终可忽略，不设置 `scene_complete`、不选路线、不制造“今天必须决定”、老师催促或虚构截止日期；
- Evaluator 对结构化目标采用对称判定：全部必需目标已有正式证据时强制完成，缺少证据时拒绝模型误报完成；Runtime 已锁存完成时继续保持完成，Actor 不再接收已完成目标；
- 数值判定会检查整个已提交 Ledger：同一 Session 中，完全相同的玩家输入不能再次命中同一 metric criterion；关系数值原有四回合冷却继续独立生效，不影响不同表达产生新证据；
- 开场、普通回合和换场只接收各自所需的输出协议；换场共享硬边界只发送一次，来源回应按路线原因、必须保留事实和语气合同解释本轮选择，目标开场仍由 Runtime 确定性注入；
- 没有增加 Planner、Repair、第二次 Actor、额外评分模型或语义关键词正则。

#### 2.46 剧本包修复

- 《毕业铃响前的半步》正式安装包更新为 `draft_12`，hash 为 `sha256:43c63bab97f87dac859bf1ecab7e28b8bf43c18f5a7fec422f9cc78e78902ce0`；编译和 N.E.K.O 校验均为零 warning；
- 天蓝色手账只包含三项明确约定和空白末页；学校任务单、旧校舍影像与档案资料是另一组独立材料。各幕目标、开场、摘要和关键物品边界使用同一事实，不再让 Actor 猜测二者关系；
- 普通结局明确为双方主动选择暂不定义关系、保持各自舒服的节奏，不是男主拒绝、女主受伤后接受。来源回应和目标幕合同同时禁止强颜欢笑、指节泛白、垂眼失落、服从或单方面牺牲。

#### 2.47 验证结果与残余边界

- Numeric v2 模型、Runtime、合同和 Router 回归共 176 项通过，胶囊、页面与浏览器恢复链路另有 30 项通过；实际 Prompt 在第 8 回合为 4576 Token、第 15 回合为 4656 Token、普通结局换场为 4596 Token，均低于 4800 Token；
- 连续 15 回合探针中，玩家持续拒绝主线时 Runtime 没有强制换幕，最终 `revision=15`、Ledger 15 条且恢复一致；第 8 回合后每个成功回合均至少提供一个主线入口；
- 最终定向压测包括 5 次开场、10 次关键物品自然话题、5 次第 15 回合收束和 8 次普通结局换场。没有一次把照片、涂鸦或新增条目写进手账；一次“里面的回忆”只是对既有三项约定的比喻，不构成关键内容编造；5/5 收束均无最后通牒或虚构截止日期；
- 普通结局首轮 8 次调用中有 7 次有效、1 次不合法换场字段；随后同换场点追加 12 次原始结构探针全部成功。合计 19 次有效输出均表现为共同选择、轻松或坦然，没有受伤、服从或强颜欢笑；唯一非法输出被严格合同拒绝，正式 Session 不会提交半回合。该失败说明单 Actor 自由文本仍有格式概率，不通过第二次 Actor 或 Repair 掩盖，人工测试时应同时观察错误回退体验。

### 问题 24：开机空白节点的推荐把猫娘信息需求倒置给男主

状态：**通用代码已修复；开场与连续普通回合真实模型定向复测通过，待新 Session 前端人工验收**

#### 2.48 证据与根因

《霓虹下的猫耳重构》正式 Session `numeric_capsule_session_9ed8cb3a-a1af-4575-abf6-e5278b698ac0` 的开场只由猫娘说出“视觉校准完成”和“主存储区为空”，第一条推荐却是男主输入“这里是哪里？你是谁？”。玩家点击后，猫娘又正确地追问“这里是什么地方？你又是谁？”，形成角色职责倒置和紧邻复读。

第一次修复只约束 `opening` 调用。随后新 Session `numeric_capsule_session_58fbe9f9-59ea-4c9b-965f-dafe7720559e` 的开场推荐已经正确给出“这里是安全屋，我是你的修复师”，但第一个普通回合又生成“外面警笛声很急，我们需要立刻转移吗？”。当前节点仍是同一个 `fresh_boot + empty memory` 开机节点，证明真正作用域应是当前可见节点的认知合同，而不是 Actor 的开场/普通回合调用阶段。

普通回合的主线推荐已经按结构化目标 `owner` 分流，但开场没有普通回合的 owner 分流合同。Actor 因此可能把 `acting_contract.allowed_behaviors` 中属于猫娘的“询问地点或身份”复制到男主候选。推荐输入解析器此前只检查 JSON、可直接发送格式和占位符，不判断这种自由文本角色语义。

#### 2.49 解决方案与边界

- 开场继续使用独立的 `opening_player_reply` 合同：猫娘的提问、观察、自检、动作和台词由 `performance` 执行；每条候选是男主对已经显示正文作出的下一步回应；
- 推荐问句权限改为按当前可见节点计算：普通回合看来源节点，换场看 Runtime 已选定的目标节点。只要可见节点结构化声明 `cognition_state=fresh_boot` 且 `memory_state=empty`，开场、普通回合和换场后的推荐都只允许男主陈述、主动披露、明确决定或非接触动作；不从“失忆、刚启动”等自然语言猜状态；
- Actor 输出解析器在该状态下统一剔除带 `? / ？` 的候选，并继续剔除“保持距离，询问……”一类不能原样发送的编辑式指令；Prompt 同时明确无问号的祈使式信息索取也属于角色倒置，但自由文本同义表达不宣称百分之百确定性语义硬拦截；
- 换场新增推荐约束时仍把 `factual_guard → acting_context → transition_contract` 保持在输入末尾，未削弱既有事实、人格、关系和过渡合同优先级；
- 普通认知节点继续允许男主自然提问，玩家输入框在任何节点都允许自由输入。没有增加 Planner、Repair、第二次 Actor、额外评分模型或关系语义关键词正则。

#### 2.50 验证结果

- 第一轮只加强 Prompt 后，同一霓虹开场 20 次真实调用仍有 3 次生成“这里是哪里”类倒置候选，证明不能把 17/20 当作通过；
- 增加结构化开机状态的确定性问句边界后，同一安装包重新生成开场 20 次，角色倒置命中为 0/20，每次仍保留 2—4 条男主陈述或动作候选；猫娘需要询问时均先在 `performance` 中问出，候选随后回答或披露；
- 节点作用域修复后的最终隔离压测固定使用上述人工复现输入，运行 5 条双回合轨迹，共 10 个普通回合、33 条保留候选：模型调用错误 0、候选为空回合 0、显式问句 0，人工检查也未再发现把地点、身份、风险判断或行动方案反问给猫娘的候选；
- Actor 定向回归 95 项通过，Numeric v2 模型、Runtime、合同和 Router 核心回归共 185 项通过；普通认知开场与普通回合保留玩家问句的兼容用例、开机节点普通回合、换场目标节点和字段顺序回归均通过；
- A/B 压测同时证明，单纯追加更长的“不得编造事实”提示不能稳定减少推荐中的具体事实漂移，因此该无效 Prompt 已撤回，避免浪费 Token 或伪称解决。关键物品事实仍服从问题 22 的既有合同和“单 Actor 概率残余”边界，本问题只闭环角色职责；
- 已提交的旧 Session 不会被重写。人工复测需要重启后端并对该剧本执行“重新开始”，让新 Session 使用当前代码重新生成开场与普通回合。

### 问题 25：现有三个剧本首幕目标过密，内容可读但实际演绎容易空转

状态：**三个剧本包与生成器项目已收口，真实模型隔离压测通过；进入正式前端人工体验**

#### 2.51 证据与内容侧根因

本轮不再以“单次生成零瑕疵”为目标，而以“剧本能继续玩、每幕有事件、内容不枯燥”为停止线。首轮真实 Actor → Evaluator → Runtime 隔离压测暴露的主要问题不在节点数量或结局数量，而在首幕把饮水、工具检测、身份复述、环境闪烁和任务说明等可选互动拆成过多阻塞目标：森林反复确认温水与稳相环，校园连续九回合安排分工仍不换场，霓虹的跨回合精确锚点还会触发目标证据错误。强行增加回合只会掩盖目标无法闭环，并不能改善演绎。

#### 2.52 三个剧本的最小内容收口

- 三个首幕都只保留三个关键目标，第一幕节奏统一收敛为 `min_turns=4 / recommended_turns=6`；饮水、工具观察、普通回忆和氛围互动继续允许即兴，但不再作为换场闸门；
- 《霓虹下的猫耳重构》更新为 `draft_23`：雨夜加入断网终端的无来源握手请求，身份由男主披露事实闭环，失忆原因保留为长线未知；身份确认后先处理终端隔离和过夜安全，不再反复追问记忆原因，也不通过列举禁用内部部件名称反向诱导 Actor；
- 《月影森林的四叶契约》更新为 `draft_14`：温水和稳相环改为可选互动，女主对检测只作一次自主选择；远处可见蘑菇村菌盖微光，同行提议不再依赖恢复记忆；第二幕提前交付四叶旧徽记与裂开的冬叶，让剧名钩子尽早进入可见剧情；
- 《毕业铃响前的半步》更新为 `draft_16`：任务单明确校园文化节截止时间、学校选择两人的原因以及校方资料与天蓝色手账的边界；首幕只要求选择第一条旧照片线索、确定双方具体分工和共同现场时间，老香樟树、照片年代与共同前往现场保持同一事实；
- 三个安装包的最终 Story 快照已原样同步回对应生成器项目，只递增项目 revision，保留 `setup.content_boundaries`、其他高级选项、作者信息和编辑器节点布局；未修改 Actor、Evaluator、Runtime，也未增加模型或重试。

#### 2.53 验证结果与停止线

- 三个最终包均通过 `NumericV2Compiler`，零 warning、无重复目标 ID、无缺失路线目标；开场固定上下文分别为霓虹 4084、森林 3832、校园 4159 Token，均低于 4800 Token；
- 真实模型使用隔离内存 Session，不写正式 Store：霓虹 `draft_23` 用 4 回合完成雨夜身份确认、终端隔离并换到次晨；森林 `draft_14` 用 5 回合完成施法失败与有限同行并进入蘑菇村；校园 `draft_16` 用 7 回合完成线索、分工与时间确认并进入香樟树场景；三次最终链路均无 Actor/Evaluator 错误；
- 最终 package hash 分别为霓虹 `sha256:134be5677216146fa75cd2a03971c695ec6e1b8170b68e86da0e462e99a45d36`、森林 `sha256:4fd1480636bc6e0627763ddcd9b644cfc27ed23c92c241bbd3455357f96b7153`、校园 `sha256:6f84a7ac283773fc802d518422aa0209a68ead340e2d3ff2e9433d629603ff29`；
- 每轮压测后均由多名独立 agent 从剧情、目标闭环、角色归属和产品停止线复盘。最终接受的概率残余包括少量动作模板、一次主线提议复述、校园分工语气偶尔偏顺从以及个别非关键推荐发散；它们不阻塞节点、不改 Runtime 事实，也不再作为继续堆 Prompt 或新增模型的理由。后续质量判断转入新 Session 的真实前端人工长程演绎。

## 3. 已闭环问题

下表归并此前 Numeric v2 正式质量闭环和健壮性修复。详细旧轮次流水已删除，保留能指导后续回归的结论。

| 问题 | 根因 | 解决方案与验证 | 状态 |
| --- | --- | --- | --- |
| 未来节点摘要被当成已发生事实 | Prompt 上下文没有严格区分 `recent_context` 与 `pending_goals` | Actor/Evaluator 明确以已提交表现为唯一已发生事实，目标只表示待发生；真实回放未再倒写未来事实 | 实测通过 |
| 达到 `min_turns` 后即使幕目标未完成也硬切 | Runtime 把最少回合误当成自动完成条件 | 增加 `scene_complete`，达到最少回合但未完成时返回 `scene_incomplete`；支线停留与回流回归通过 | 实测通过 |
| 玩家纠正误解却被负向扣值 | 判定过度依赖语气或表面词汇 | Evaluator 只能引用作者 criterion，并结合当轮原话与当前场景证据；负向判定回归通过 | 实测通过 |
| 开场替玩家执行动作或制造隐形前史 | 开场直接照演节点摘要 | 开场使用独立 Actor 合同，只建立环境和猫娘第一句，不假定玩家说过或做过任何事 | 实测通过 |
| 玩家行动、条件意愿被 Actor 写成已完成事实 | 旁白权限和角色归属不够明确 | 旁白只写猫娘和可见环境；玩家原话由前端单列；“可以、如果、打算、改天”不能推导为已完成行动 | 实测通过 |
| 候选男女主原名覆盖当前称呼与猫娘名 | 作者身份与运行时角色卡没有统一投影 | `numeric_v2_cast.py` 和角色绑定把男主投影为玩家称呼、女主投影为当前猫娘名；候选原名不进入演绎 | 实测通过 |
| Actor 某个 dialogue item 非法导致脏 speaker | 输出校验粒度不合适 | 旧版曾过滤非法 item；当前有序内容合同改为整份严格校验，错误 speaker 不会被重分配或静默提交 | 自动回归通过 |
| 终局已到达但 Numeric 页面不显示结局 | 前端没有消费 `scene.ending` | 增加结局标题/摘要面板并关闭输入；浏览器回归和新 Story ID 真实复测通过 | 实测通过 |
| 关闭重启后从头开始 | 页面活动指针和服务端恢复槽位不完整 | 以 `Story ID × character_id` 恢复唯一槽位，关闭不结束 Session；重启恢复回归通过 | 实测通过 |
| 同一剧本累积多个可恢复 Session | 结束记录、活动指针和重新开始语义混杂 | 每槽位只保留一个 Session；重新开始使用新 ID 原子替换旧文件；并发、重复提交和跨剧本恢复回归通过 | 实测通过 |
| 服务端已提交但响应丢失后，胶囊重试暂时缺少猫娘回复 | 后端幂等重放只返回权威快照，前端却仍按首次成功响应等待单条 `performance` | 胶囊识别 `idempotent_replay` 后直接用服务端快照重建完整历史并移除乐观气泡；浏览器模拟连接重置后使用同一 `client_turn_id` 重试通过 | 浏览器回归通过 |
| 换场后 Evaluator 单条上下文超过输入预算 | 三段式换场被整条投影为当前幕事实，唯一记录又不能按完整回合淘汰 | Session 仍保存完整换场；Evaluator 只读取目标幕 `target_opening`，不重复发送旧幕回应、换场桥和旧玩家输入；三个真实剧本连续换场压测未再报预算错误 | 实测通过 |
| Evaluator 偶发返回剧本不存在的数值 ID，导致整轮失败 | 模型偏离 `metric_schema` 时解析器把无权限写入的未知项当成整份结果错误 | 未知数值项确定性忽略并记录告警，合法数值和 `scene_complete` 继续生效；未知项永不进入 Runtime 或 Ledger，自动回归及真实后续回合通过 | 实测通过 |
| 角色改名或同名新角色可能串进度 | 过去依赖展示名称绑定 | 改用角色卡不可变 ID；改名保留进度，同名新 ID 不继承，删除角色按 ID 级联 | 自动回归通过 |
| 删除剧本或角色可能留下无主 Session | 包、Session 与索引分别删除 | 使用可恢复删除事务和冷启动审计；损坏、无主、重复文件进入最多 6 份隔离区 | 自动回归通过 |

## 4. 正式前端质量闭环摘要

截至 2026-08-24，Numeric v2 已完成十七轮主题或定向压测。第 6 轮使用当时三个安装包和真实模型 API，重点检查胶囊聊天中的演绎效果、推荐输入和页面交互；第 7—11 轮逐步收口人格、归属、推荐语、目标证据和换场重复；第 12 轮集中验证关系数值节奏、Actor 预算与支线换场边界；第 13 轮验证推荐输入在建议回合数前发散、到点后拉回主线，以及关键物品与非关键即兴的分层边界；第 14 轮按最终产品语义验证第 8 回合起持续保留主线入口、玩家拒绝时不强制换幕，并复测校园剧本的关键物品和普通结局情绪；第 15 轮修复开场推荐中的猫娘/男主职责倒置；第 16 轮根据真实人工 Session 把该边界扩展到同一开机节点的普通回合和换场目标；第 17 轮按“能玩且不枯燥”的产品停止线收口三个现有剧本的首幕内容与节奏。

| 轮次 | 主题与范围 | 结果 | 主要结论 |
| --- | --- | ---: | --- |
| 第 1 轮 | 新包主线基线 | 不合格 | 暴露未来事实、身份和节奏问题 |
| 第 2 轮 | 角色与节奏修复 | 不合格 | 仍有玩家归属和当前输入响应问题 |
| 第 3 轮 | 开场事实与负向判定修复 | 不合格 | 主合同收敛，但 Actor 输出质量仍需收口 |
| 第 4 轮 | 木作主题、数值支线和主线回流 | 96/100 | `scene_complete`、支线停留、回流、隐藏数值和刷新恢复通过 |
| 第 5 轮 | 雾港灯塔、双支线与 Normal 结局 | 修复前阻断；修复后 93/100 | Runtime 正确到达终局；补齐结局面板后，26 回合、双支线、刷新恢复和结局展示通过 |
| 第 6 轮 | 当前三个安装包，演绎效果与交互体验 | 72/100，不通过 | 41 个成功模型回合；一次目标开场合同失败；猫灯复测 12 回合未换幕；开场推荐语串线；普通回合冗长和人格覆盖仍存在 |
| 第 7 轮 | 修复后三剧本 Evaluator → Runtime → Actor → 原子提交链路 | 定向复测，不作前端总评分 | 24 个成功回合、8 次换场、2 条隐藏支线；换场可靠性和云舒镇餐次通过，但发现人格、推荐语、玩家归属、候选名、Evaluator 主体归属和换场重复问题 |
| 第 8 轮 | 问题 6、12—16 修复后的真实模型链路 | 定向复测通过，不作前端总评分 | 三剧本主复测 25 个成功回合、8 次换场、2 条隐藏支线且零失败；猫灯拖幕从 10 次降为 3 次；最终猫尾特调补测 8 回合零失败 |
| 第 9 轮 | 新版《猫尾特调恋爱指南》替换、复合目标与支线回接 | 定向复测通过，不作前端总评分 | 14 个成功回合、4 次换场、隐藏支线 4 回合完成；三项经营边界均由猫娘对白直接交付，零模型失败，恢复一致 |
| 第 10 轮 | Prompt 去剧本化后的泛化回归 | 定向复测通过，不作前端总评分 | 11 个成功回合、4 次换场、隐藏支线 4 回合完成；零模型失败、零高相似推荐语，未泄漏任何结构示意内容 |
| 第 11 轮 | 推荐输入语态与演绎效果；复合目标、换场来源复读修复 | 首轮发现问题，定向修复复测通过 | 首轮 3 个开场、16 回合、3 次换场、35 次模型调用零失败；修复后猫尾特调五项条款完整交付才换场，推荐台词统一中文引号，来源回应未复读 |
| 第 12 轮 | 关系数值强度、目标证据、Actor 长场景预算和三处支线换场 | 定向复测通过，不作前端总评分 | 新 Session 10 回合在第 8 回合换幕且关系保持 guarded；泛化行为不再误加能力值；三处支线换场合同成功；后续以阶段化响应合同替换了不可穷举的关系关键词硬校验 |
| 第 13 轮 | 推荐输入发散与到点拉回、关键物品事实保护 | 定向复测达到可用，不作前端总评分 | 月影森林关键圆环保护与第一条主线推荐均为 5/5；霓虹目标主体修复后主线推荐为 4/5，保留单 Actor 概率残余；相关自动回归 200 项、Electron smoke 2 项通过 |
| 第 14 轮 | 最终回合节奏语义、完成判定、重复刷值和《毕业铃响前的半步》 | 定向复测通过，进入人工验收 | 连续 15 回合拒绝主线未被强制换幕，第 8 回合后持续提供主线入口；关键物品具体编造 0/15，收束强迫 0/5，普通结局有效输出 19/19 情绪合格；20 次中唯一非法换场结构被原子提交拦截；核心回归 176 项、前端与浏览器回归 30 项通过 |
| 第 15 轮 | 霓虹开场推荐的角色归属 | 定向复测通过，进入新 Session 人工验收 | Prompt 单独修复仅 17/20；增加结构化开机空白态问句边界后，同一开场 20/20 无角色倒置且每次保留候选；核心回归 180 项通过 |
| 第 16 轮 | 开机节点连续普通回合的推荐角色归属 | 定向复测通过，待新 Session 前端人工验收 | 人工 Session 证明 opening-only 作用域不完整；改为按当前可见节点判断后，最终 5 条双回合轨迹共 10 回合、33 条候选无角色倒置且无空候选；核心回归 185 项通过 |
| 第 17 轮 | 霓虹、森林、校园三个现有剧本的内容可玩性 | 三部首幕均完成换场，进入正式前端人工验收 | 首幕阻塞目标统一收敛为三项；霓虹 4 回合、森林 5 回合、校园 7 回合完成首次换场，最终链路零模型错误；保留不影响推进的单 Actor 概率小瑕疵，不再继续堆叠 Prompt |

第 5 轮复测关键证据：

- Story ID：`story_6d7e4f90a1b2`
- Session ID：`numeric_web_session_05bd522a-f993-4180-9f00-6c84f3f97d02`
- 26 个真实模型回合，最终 `revision=26`、`status=ended`、`current_node_id=ending_normal`
- 路线：`mainline_01 → mainline_02 → branch_trust_archive → mainline_03 → branch_clue_signal → mainline_04 → ending_normal`
- 刷新后完整历史、结局标题和摘要仍存在，输入与推荐输入关闭
- 最终评分 93/100，无阻断问题

这些结果证明既有包可以完成主线、支线、回流和终局，但不证明所有新剧本的 Actor 换场都可靠。2026-08-14 的问题 1 正是新的反例，因此换场合同需要独立补强。

第 6 轮补充证据：

- 《云舒镇的奇遇》8 回合完成两次换场，普通回合最长 78 字；
- 《猫尾特调恋爱指南》10 回合完成两次换场，但普通回合最长 147 字，早餐幕明显复读；
- 《月台尽头的猫灯》第一次运行完成 11 回合后在第 12 回合换场失败；第二次运行 12 回合仍停在第一幕；
- Evaluator 与 Actor 合计平均等待约 5.0—5.8 秒，单轮最慢 8.4 秒，之后还要进行前端打字机显示；
- 真实选择页可见三个剧本，未开始剧本的“开始”可用、“继续”禁用，已退出剧本的“重新开始”和“继续”均可用；胶囊聊天和选择页浏览器回归 11 项通过；
- 压测 Session 全部使用隔离临时目录，正式 Session 数量保持为 3；本轮没有验证 TTS，也没有把 TTS 状态计入评分。
- 第 6 轮继续定向压测共生成 12 次开场和 12 次同状态换场：非云舒镇开场的业务示例污染率为 7/8；猫灯目标开场合同失败率为 7/12；5 次成功换场均暴露目标摘要替玩家决定“午夜巡视”的归属错误。

第 7 轮补充证据：

- 全部使用当前小葵角色卡、真实 Evaluator/Actor API、真实 Runtime 原子提交和临时 Session 目录；正式 Session 数量未变化；
- 《云舒镇的奇遇》5 回合完成 `mainline_01 → mainline_02 → mainline_03`，两个节点分别停留 3/2 回合，普通回合最长 76 字，餐次和秋刀鱼事实一致，恢复后的 revision 与 Ledger 数量一致；
- 《猫尾特调恋爱指南》12 回合完成 `mainline_01 → mainline_02 → mainline_03 → node_branch_44a330dc73`，三个节点分别停留 3/5/4 回合，普通回合最长 144 字，早餐幕仍复读，支线泄漏“米娅”并预设玩家展示计划书；
- 《月台尽头的猫灯》7 回合完成 `mainline_01 → mainline_02 → mainline_03 → node_branch_3075b87179`，三个节点分别停留 3/2/2 回合，普通回合最长 65 字，支线可达且恢复一致；但第一幕在猫娘未说出 `9月17日` 时已被 Evaluator 判定完成；
- 共 24 个成功回合、8 次成功换场，平均单回合模型链路等待约 5.7—6.4 秒；没有 Actor 结构失败、revision 冲突或半回合；
- 本轮没有操作真实前端、TTS 设备或记忆写入弹窗，因此不对这些维度评分，也不能据此确认记忆功能正常。

第 8 轮补充证据：

- 首次修复验证暴露猫灯第一幕因 Evaluator 过度保守而停留 10 次，并产生 2 次 `numeric_v2_actor_repeated_output`；该结果未提交失败回合，随后通过目标 owner 与“已全部交付必须完成”合同修正；
- 最终三剧本主复测共 25 个成功回合、8 次换场、2 条隐藏支线，全部 Session 恢复后的 revision 与 Ledger 数量一致，没有 Actor/Evaluator 失败、revision 冲突或半回合；
- 《云舒镇的奇遇》用 9 回合到达 `mainline_03`，两个节点分别停留 3/6 回合；小葵保留警惕和边界，但不再出现“咬你、饶不了你”等威吓；
- 《猫尾特调恋爱指南》用 8 回合进入改革支线，三个节点分别停留 2/4/2 回合；候选名“米娅”和第三人称推荐语均未再出现；修正“前情已签约、第一幕又要求签约”的身份矛盾后，最终独立补测 8 回合连续成功；
- 《月台尽头的猫灯》用 8 回合进入车厢支线，三个节点分别停留 3/2/3 回合；玩家首轮提供 `9月17日` 后，猫娘仍需亲口确认目标事实，未提前换幕，也未擅自生成爱人姓名；
- 开场、普通回合和换场推荐语的操作说明命中为 0；目标开场仍由 Runtime 唯一注入，桥段未再逐句复述同一开场；
- 三个项目的节点、路线、数值条件和优先级结构未改变；安装包原子替换后，正式 Session 仍为 3 条，仅同步兼容包哈希，状态、节点、revision、历史和 Ledger 均保持不变；
- 本轮仍未操作真实前端、TTS 设备或记忆写入弹窗；问题 10 按产品决定保留软字数目标，不把少量超字视为失败。

第 9 轮补充证据：

- 旧 Story ID `story_b784404f3674` 已从正式安装包和 Session 索引移除并保存在可恢复备份中；新版 Story ID 为 `story_f8f5f2096de1`，安装修订为 `cocktail_contract_v2_retest9`；
- 最终隔离链路用 14 个成功回合到达 `mainline_04`，节点停留为 `5/3/2/4`，路线为 `mainline_01 → mainline_02 → mainline_03 → node_branch_10a057fc38 → mainline_04`；
- 支线三个硬目标分别由猫娘对白说出，没有再用玩家提案或含糊确认补齐；最终轮没有 Actor/Evaluator 失败、revision 冲突或半回合，恢复后的 revision 与 Ledger 一致；
- 有 1 轮推荐输入与近期候选高度相似。当前过滤器在全部候选重复时保留一条可点击兜底，因此这是非阻断体验项；本轮没有为消除该统计而让推荐区强制变空。

第 10 轮补充证据：

- 生产 Prompt 已移除中介租房、`9月17日旧信`和固定羞辱坏句等现有剧本来源示例；推荐输入改用无业务事实的结构示意，生成器示例明确禁止复制示意中的物品、编号、地点和时间；
- 未重新编译或改写剧本包，直接用相同新版包验证 Prompt 泛化，11 个成功回合到达 `mainline_04`，节点停留为 `2/3/2/4`；
- 隐藏支线仍按“一月、一个实验区、保留原经营方式”逐项交付后回接主线，输出没有出现“记录、检修单、B-12、闸门、18:30”等结构示意污染；
- 本轮没有 Actor/Evaluator 失败、revision 冲突、半回合或高相似推荐语，恢复后的 revision 与 Ledger 一致；仍未覆盖真实前端、TTS 设备和记忆写入弹窗。

第 11 轮补充证据：

- 首轮使用当前三个安装包和小葵角色卡生成 3 个开场，并对云舒镇、猫尾特调各连续演绎 8 回合；Actor 与 Evaluator 共 35 次真实模型调用，零请求失败，临时 Session 销毁后正式记录不变；
- 云舒镇 8 回合完成 2 次换场，猫尾特调 8 回合完成 1 次换场；59 条推荐输入没有机械“我+动作”，但暴露英文引号、动作意图摘要、猫尾特调复合条款提前完成和来源回应复读；
- 修复后猫尾特调使用新临时 Session 重跑第一幕：第 2 回合交付三个月、岗位、每日八小时和每周休息两天，第 3 回合交付实际损失赔偿与无翻倍违约金，第 4 回合签约并换场；
- 修复后开场及 4 个回合的推荐台词全部使用中文引号，动作项没有机械玩家主语或意图摘要；换场来源回应为新生成的简短承接，没有照搬上一轮；
- 本轮仍未操作真实前端、TTS、记忆写入弹窗和隐藏支线，不把定向结果替代完整前端质量评分。

第 12 轮补充证据：

- Evaluator 使用有限强度并由服务端按作者限幅换算 delta；关系相同依据的最近奖励随 Ledger 一起进入判定，目标证据按每项目标最近四条、全幕最近八个 revision 保留，并在本幕完成锁存或换场后清空；
- Actor 4800 Token 输入预算不再截断稳定背景、身份、当前输入和剧情合同；超限时先删除防重复辅助字段和最早完整历史，换场再删除已消费来源目标及重复风格说明。修复前第 8 回合换场因固定上下文超限失败，修复后同一长场景成功换幕；
- 连续 10 回合真实模型链路最终关系数值为 2、阶段仍为 guarded，非关系能力没有因“观察、检查安全”被误奖励；Session revision、Ledger 和换场节点一致；
- 三处支线阈值状态的真实 Actor 换场探针全部返回合法 `source_response → transition_bridge → target_opening`；最低关系改为阶段化响应合同后，最新 5 次真实 Actor 抽测未出现讨好式服从，但自由文本语义不再被描述为关键词硬校验；
- 自动回归覆盖 N.E.K.O Numeric v2 模型、Runtime 和 Router 相关 106 项，生成器 Numeric v2 相关 52 项；本轮未操作真实前端、TTS 设备和记忆写入弹窗，不把定向结果替代完整前端质量评分。

第 13—16 轮补充证据：

- `min_turns` 前的普通态和聚焦态继续使用发散推荐；达到最短推进回合且存在未完成目标时切换为 `mainline_pull`，达到建议回合数后切换为 `mainline_closure`；两者都不会设置 `scene_complete` 或直接选路；
- 月影森林首次收束探针暴露关键工具属性被补造，分层事实合同修复后连续 5 次未再补造圆环属性、名称、功能或效果，第一条候选均回到尚未完成的工具目标；
- 霓虹最终 5 次探针有 4 次正确形成“猫娘提问、男主推荐回答”，1 次仍倒置问答角色；普通雨声、环境和非关键技术化细节按本轮产品决定不计失败；
- 校园剧本的连续 15 回合拒绝探针没有被强制换幕，最终 revision、Ledger 和恢复结果一致；普通结局 20 次调用中 19 次合法且情绪合格，唯一非法结构没有提交；
- 霓虹最新正式 Session 证实开场推荐把猫娘问题倒置给男主；只加强 Prompt 后仍有 3/20 复现，增加基于 `fresh_boot + empty memory` 的确定性问句边界后复测为 0/20；
- 后续人工 Session 又证明同一开机节点进入普通回合后会重新出现倒置；边界改为跟随当前可见节点后，最终双回合探针 10/10 未复现，且普通认知节点仍保留提问能力；
- 正式包和既有 Session 哈希同步后可恢复；自动回归、浏览器与桌面 smoke 全部通过。本轮仍以真实 Actor 固定状态和隔离连续 Session 为主，不替代下一轮正式前端人工长程验收。

## 5. 后续实测协议

每一轮正式压测必须使用新的 Story ID 与新的 Session ID，避免旧包、旧 Session 或旧模型输出污染证据。建议至少包含：

- 4—6 个主线节点；
- 至少一条隐藏数值支线并回到指定主线节点；
- 至少一个可达结局；
- 一次推荐输入点击，其余使用玩家自由自然语言；
- 一次刷新恢复；
- 一次明确的时间或地点换场；
- 一次“休息、离开或结束交谈”类输入；
- 一个身份纠正、条件意愿或拒绝类输入；
- 到达终局后验证结局面板和输入关闭。

评分维度保持为：

| 维度 | 分值 |
| --- | ---: |
| 玩家输入响应与因果 | 25 |
| 连贯性、事实与身份 | 20 |
| 节点节奏与转场 | 20 |
| 旁白与对白关联 | 15 |
| 数值判定正确性 | 10 |
| 前端与 Session 可靠性 | 10 |

总分达到 85 且没有阻断问题才算通过。以下任一项直接阻断：

- 正式节点与可见场景不一致；
- 身份交换或候选原名重新出现；
- Actor 替玩家完成关键行动；
- Runtime 越过未完成幕目标；
- 隐藏数值或路线阈值泄漏；
- 同一输入重复提交、半回合或 revision 链断裂；
- 刷新后历史、当前节点或结局丢失；
- 剧场模式与普通聊天串用输入、草稿、历史、音频或恢复状态。

归因和修复必须保持固定顺序：

1. 先检查 Story Package 的节点目标、路线、过渡合同和可达性；
2. 再检查 Session、Ledger、revision 与表现历史是否一致；
3. 再查看 Evaluator 和 Actor 实际获得的上下文以及原始输出；
4. 再检查 Runtime、Store 与 Router 是否按合同结算和提交；
5. 最后检查前端是否正确投影服务端结果；
6. 只修仍可复现的归因层，不用 Prompt 补丁掩盖结构或持久化问题；
7. 修复后先跑直接自动回归，再用新 Story ID 和新 Session 做真实前端复测。

## 6. 已知但暂不扩展的风险

- 单个 Session 的 Ledger 和表现历史会持续增长，当前没有轮数或体积上限；等待实际容量证据后再设计归档。
- 真实桌面 TTS 设备发声仍需人工验收；自动测试只能证明已提交对白进入共享桥且失败会降级为文字。
- 生成器用户模型配置可能被测试地址污染，或因 provider base URL/API 协议不匹配导致生成失败；该问题属于 `NEKO_Numeric_drama` 配置隔离与兼容层，应在生成器仓库验证，不在 N.E.K.O Runtime 中增加 provider 特判。
- 引导式支线生成属于作者工作流，不得把生成失败重试、续写或草稿恢复逻辑带入玩家 Runtime。
